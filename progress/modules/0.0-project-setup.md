# Module 0.0 — Project Setup

**Status:** blocked (folder structure + tooling done; blocked on external account creation — see Blockers)
**Phase:** 0 (see docs spec Section 10 for phase timing)
**Spec reference:** Section — of AEO_Visibility_Platform_Spec_v1.1.docx
**Depends on:** none
**Blocks:** 5.1
**Owner:** unassigned
**Last updated:** 2026-07-23

## Purpose
Repo, hosting, and service accounts scaffolded so every later module has somewhere to run.

## Acceptance criteria
- [x] Monorepo created with the folder structure in docs/CONVENTIONS.md
- [ ] Supabase project created, RLS enabled by default on the schema
- [ ] Vercel project connected to the repo
- [ ] Razorpay test-mode account created
- [ ] Gemini + NVIDIA NIM API keys obtained and stored as server-only env vars
- [ ] Domain purchased/pointed (or a working *.vercel.app placeholder)

## Tech
Git, Supabase, Vercel, Razorpay, Google AI Studio, NVIDIA Developer Program.

## AI usage
None.

## Caching
N/A at this stage.

## Security
Establish the env var naming convention (server-only vs NEXT_PUBLIC_) here, on day one, so no module after this one gets it wrong.

## Files touched
- `app/` — full Next.js project (package.json, tsconfig.json, eslint.config.mjs, .prettierrc.json, .prettierignore, next.config.ts, postcss.config.mjs, .env.example, .gitignore, README.md)
- `app/src/app/` — layout.tsx, page.tsx, globals.css (default template trimmed, Google Fonts removed)
- `app/src/modules/{auth,brand-config,engine-query,nlp-extraction,scoring-explanation,crawl-audit,alerting,billing,admin,free-check,growth-automation}/index.ts` — barrel-export stubs, one per module
- `app/src/lib/{ai-providers,cache,db,security}/index.ts` — stub folders per docs/CONVENTIONS.md
- `app/src/types/index.ts` — shared types stub
- `app/tests/unit/sanity.test.ts`, `app/tests/integration/.gitkeep`, `app/tests/package.json` (scopes ESM to tests/ only)
- `app/supabase/migrations/.gitkeep`
- `.github/workflows/ci.yml` — lint + typecheck + test on push/PR
- `.github/dependabot.yml` — npm (app/) + github-actions, weekly
- `.gitignore` (repo root)

## Decisions log
_(append-only — date, what was decided, why. Do not delete old entries even if superseded; strike through instead.)_
- **2026-07-23** — Scaffolded `app/` per docs/CONVENTIONS.md Section 1: Next.js 16.2.11 (App Router, TypeScript strict, Tailwind), module/lib/type stub folders, tests/, supabase/migrations/, .env.example (documents the server-only vs NEXT_PUBLIC_ convention plus the multi-key-pool env vars from docs/CONVENTIONS.md Section 5), ESLint (next/core-web-vitals + eslint-config-prettier) + Prettier, and the CI workflow + Dependabot config. Verified clean in this session: `tsc --noEmit`, `eslint .`, `prettier --check .`, and the test suite.
- **2026-07-23** — Testing tooling: used Node's built-in test runner (`node --test`, native TS type-stripping — zero extra dependency) instead of Vitest. Vitest reproducibly crashed with a native "Bus error" tied to its worker-pool/native-binary startup, reproduced across `--pool=threads` and `--pool=forks`, across Vitest v4 and v2, and with `useAtomics=false` — isolated to this build shell specifically (`node --test`, plain `worker_threads`, and plain `child_process.fork` all ran fine standalone). Documented so a future session doesn't re-add Vitest expecting a different result in the same environment without re-verifying first.
- **2026-07-23** — `next build` could not be verified to completion in this sandboxed shell, for the same underlying reason: Next's production-build worker process also crashed with SIGBUS, reproduced on both Next 15.5.21 and 16.2.11 (ruling out a version-specific bug) and independent of `experimental.cpus`/`workerThreads` settings. `next dev` (same SWC compiler, no build-time worker pool) ran fine, `tsc --noEmit`/`eslint`/tests all pass — isolating the fault to that one worker-spawning code path in this environment, not a code defect. Stayed on Next 16.2.11 (latest) since the crash isn't version-specific. The `.github/workflows/ci.yml` added this session runs the real build on a normal GitHub Actions runner once pushed — treat that as the actual build gate, not a local run in this sandbox.
- **2026-07-23** — Removed `next/font/google` (Geist/Geist Mono) from the default template; it requires a build-time fetch to Google Fonts for zero product benefit at this stage, and is one more thing that can fail a production build. Using Tailwind's default font-sans stack instead. Revisit with `next/font/local` if/when there's an actual brand typeface.
- **2026-07-23** — `git init` was attempted at the repo root but hit a stuck `.git/index.lock`, caused by the same mounted-drive filesystem limitation that affected npm's install earlier in this session (rename/unlink not fully supported from this sandbox onto the mounted D: drive). Removing the lock file requires user permission, which was declined when asked — git initialization is deferred to the user's own machine. All source files are on disk and correct regardless of git state.

## Blockers
- Supabase project not yet created (needed for `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` in `.env.example`) — no MCP connector exists for Supabase (checked the connector registry), so this needs to be done directly at supabase.com by the user.
- Vercel project not yet connected — same: no MCP connector available, needs the user (or a git remote to exist first).
- Razorpay test-mode account not yet created — same: no MCP connector available.
- Gemini API key (aistudio.google.com) and NVIDIA NIM API key (build.nvidia.com) not yet obtained.
- Domain not yet purchased/pointed (a `*.vercel.app` placeholder is acceptable per acceptance criteria until then).
- Local git repository not yet initialized on a machine where it will actually persist (see decisions log) — do `git init && git add -A && git commit` from the user's own machine, then push to GitHub before Vercel/CI can do anything.

## Next steps
1. Confirm dependencies (`none`) are `done` in progress.json before starting.
2. Implement against the acceptance criteria above.
3. Satisfy every applicable item in the shared Definition of Done (progress/progress.json → `definitionOfDone`).
4. Update this file's Status, Files touched, and Decisions log, and mirror the status/percentComplete change in progress.json.
