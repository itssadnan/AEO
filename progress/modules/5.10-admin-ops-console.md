# Module 5.10 — Admin / Ops Console

**Status:** done
**Percent complete:** 100
**Phase:** 4 (see docs spec Section 10 for phase timing)
**Spec reference:** Section 5.10 of AEO_Visibility_Platform_Spec_v1.1.docx
**Depends on:** 5.3
**Blocks:** none
**Owner:** Claude / Antigravity
**Last updated:** 2026-07-29

## Purpose

Internal-only view of API quota usage and errors across all customers, and the admin surface for managing which AI model handles which task — globally and per custom-plan customer.

## Acceptance criteria

- [x] Live Gemini + NVIDIA NIM quota consumption view, broken down per key in the pool (primary/secondary/tertiary), not just per provider (verified via migration 0019 + `onAttempt` slot callback + `quota-section.tsx`)
- [x] Key-pool health view: which keys are alive vs. marked dead, when a key died, and a manual "clear dead flag" action for once it's resolved (verified via `key-health-section.tsx` + `clearDeadKeyAction`)
- [x] Displays current failoverMode ("shared" vs "emergency-only") per provider, with a visible reminder to switch to emergency-only the moment primary is upgraded to paid/higher-tier (verified via `failover-mode-section.tsx` + `setFailoverModeAction`)
- [x] A key transitioning to dead (401/403) fires an immediate alert (not just a log line) — this is the loud-failure requirement from docs/CONVENTIONS.md (verified via `ADMIN_ALERT_WEBHOOK_URL` POST hook in `engine-worker/index.ts`'s `markKeyDead`)
- [x] Error/failure log for failed checks (verified via `error-log-section.tsx` rendering `engine_error_logs`)
- [x] Basic churn signal: customers inactive 2+ weeks (verified via `getChurnSignal()` joining `workspaces` -> `workspace_members` -> `auth.users.last_sign_in_at`)
- [x] AI task config view: list every task key's current global-default (provider, model, enabled) from ai_task_configs, editable inline (verified via `ai-task-configs-section.tsx`)
- [x] Enable/disable a model for a task globally (flips `enabled` on the global-default row) (verified via `upsertAiTaskConfigAction`)
- [x] Per-workspace override: search a workspace, add/edit/remove its override row for any task key, without touching the global default (verified via `ai-task-configs-section.tsx` + `deleteWorkspaceOverrideAction`)
- [x] Every write to ai_task_configs records updated_by + updated_at (who changed what, when) — this table has no customer-facing audit trail, so the admin log is the only one (verified via `upsertAiTaskConfig`)

## Tech

Internal-only Next.js route gated by `requireAdmin()` (checking server-only `ADMIN_USER_EMAILS` allowlist). Writes to `ai_task_configs`, `ai_provider_key_health`, `ai_provider_settings`, and `engine_error_logs` go through server-side routes/actions using the Supabase service role.

## AI usage

None.

## Caching

Not cached — internal page fetched dynamically on demand (`export const dynamic = "force-dynamic"` in `app/admin/page.tsx`).

## Security

Gated at the server/API layer via `requireAdmin()`, called independently at the top of every Server Action and inside `page.tsx`. Tested and verified via unit tests in `tests/unit/admin.test.ts`.

## Files touched

- `app/supabase/migrations/0019_admin_ops_console.sql`
- `app/src/types/database.ts`
- `app/src/lib/security/admin.ts`
- `app/src/lib/security/index.ts`
- `app/src/modules/admin/types.ts`
- `app/src/modules/admin/quota-caps.ts`
- `app/src/modules/admin/queries.ts`
- `app/src/modules/admin/actions.ts`
- `app/src/modules/admin/index.ts`
- `app/src/app/admin/page.tsx`
- `app/src/app/admin/quota-section.tsx`
- `app/src/app/admin/key-health-section.tsx`
- `app/src/app/admin/failover-mode-section.tsx`
- `app/src/app/admin/error-log-section.tsx`
- `app/src/app/admin/churn-signal-section.tsx`
- `app/src/app/admin/ai-task-configs-section.tsx`
- `app/src/lib/ai-providers/key-pool.ts`
- `app/supabase/functions/_shared/key-pool.ts`
- `app/supabase/functions/_shared/gemini-provider.ts`
- `app/supabase/functions/_shared/nvidia-nim-provider.ts`
- `app/supabase/functions/engine-worker/index.ts`
- `app/.env.example`
- `docs/architecture/entity-relationship-diagram.md`
- `app/tests/unit/admin.test.ts`
- `app/tests/unit/key-pool.test.ts`

## Decisions log

- **2026-07-23** — Scope expanded to own the admin UI for `ai_task_configs` (Module 5.3's table): global enable/disable per task, and per-workspace override management for custom-plan model experiments.
- **2026-07-23** — Scope further expanded to surface the multi-key failover pool (Module 5.3) — per-key health and an immediate alert on a key dying.
- **2026-07-27** — Circular dependency identified and resolved by treating `dependsOn: [5.3]` as data dependency.
- **2026-07-27** — Implementation spec written (`progress/specs/5.10-admin-ops-console.md`).
- **2026-07-29** — A delegate model implemented the spec per `delegate-and-verify`: migration 0019 applied, `database.ts` regenerated, `key_slot` `onAttempt` hook wired across worker/providers/key-pool, `ADMIN_ALERT_WEBHOOK_URL` hook added, Admin UI + Server Actions gated by `requireAdmin()`, unit tests added, ERD updated. Self-reported clean `tsc` and 10/10 tests, commit `9a2d118`.
- **2026-07-29 — independent verification (the skill's mandatory step; self-report not trusted as-is).** Confirmed real via direct file reads: `onAttempt`/`key_slot` wiring in both `key-pool.ts` copies, both provider files, and `engine-worker/index.ts` (success and failure paths); `requireAdmin()` gating on every Server Action; the admin route and section components genuinely wired to real actions; 10/10 unit tests genuinely pass; `tsc --noEmit` genuinely clean (one run took ~45s, right at this sandbox's tool timeout — several earlier attempts looked like hangs but were confirmed via process monitoring to be real progress); `.env.example`, `database.ts`, and the ERD doc all genuinely contain the claimed additions. Found and fixed 2 real, undisclosed bugs the self-report omitted: (1) migration 0019's `create or replace function` with an added parameter created a second overload of `complete_check_job`/`retry_or_fail_check_job` instead of replacing the original — the same mistake migration 0011 already fixed once; confirmed live via `pg_proc`, fixed with migration `0020_drop_legacy_check_job_overloads.sql`, re-verified live that exactly one overload of each now exists; (2) `upsertAiTaskConfig` called `.auth.getUser()` on the service-role client, which has no session and always returns `null` — silently breaking the "every write records `updated_by`" acceptance criterion. Fixed by threading the real admin user id from the session-scoped client (resolved in the Server Action) down as an explicit parameter. Also fixed a minor barrel-import violation in `admin.ts`.
- **2026-07-29 — found and fixed a third, more serious gap: the live worker was never actually redeployed.** The Supabase `engine-worker` Edge Function was still version 8, last deployed 2026-07-25 — four days before any of this module's worker-side code existed. None of the `key_slot` capture or dead-key webhook code had reached production; it existed only as unrun source. Confirmed via the function's own `updated_at` and via a live query showing every `check_runs` row since migration 0019 still had `key_slot = null`. Asked the user for explicit permission before pushing a live production deploy (the auto-mode classifier blocked the first attempt as a real infra change); user approved. Redeployed `engine-worker` as version 9 with current source (`index.ts` + all six `_shared/*.ts` files). Verified live: triggered a real invocation via `net.http_post` (the same call `pg_cron` makes), got back `{"processed":1,"reclaimed":0}`, and confirmed the new `check_runs` row has `key_slot = 'primary'` — populated, vs. `null` on every row before the redeploy. The fix is genuinely live, not just committed. (This surfaced a separate, real Module 5.3 problem — see that module's decisions log: the live invocation still failed `rate_limited`, and the evidence points to secondary/tertiary Gemini keys never having been provisioned.)

## Blockers

None for this module's own scope — the admin console, its queries/actions, and the worker wiring are genuinely implemented, verified, and now live. (Module 5.3 has a real, separate open blocker — see that module's file — but it does not block this module's own acceptance criteria, which are about the console surfacing accurate information, including the fact that quota is currently exhausted.)

## Next steps

1. Return to Module 5.3, resolve its API key provisioning blocker (see that module's file).
