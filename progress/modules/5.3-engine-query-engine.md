# Module 5.3 — Engine Query Engine

**Status:** in_progress (98%)
**Phase:** 1 (see docs spec Section 10 for phase timing)
**Spec reference:** Section 5.3 of AEO_Visibility_Platform_Spec_v1.1.docx
**Depends on:** 5.2
**Blocks:** 5.4, 5.6, 5.10, 5.11
**Owner:** unassigned
**Last updated:** 2026-07-25

## Purpose
The core scheduler/worker: runs each active prompt against Gemini with Search grounding, on a schedule, and stores the raw result. Built behind an AIProvider interface so engines are swappable later.

## Acceptance criteria
- [x] AIProvider interface defined; GeminiProvider is the only implementation for now
- [x] ai_task_configs table + resolveTaskModel(taskKey, workspaceId) resolver owned by this module (global default row + per-workspace override, `enabled` flag, admin-only RLS — see ERD); no other module hardcodes a provider or model string
- [x] Migration seeds global-default rows for all five task keys with the values from spec Section 6
- [x] Queue-based worker pulls due (brand, prompt) pairs and calls Gemini with grounding enabled, resolving its model via resolveTaskModel("grounded_search", workspaceId) — deployed live as the `engine-worker` Edge Function (version 1, ACTIVE); not yet proven with a real end-to-end run (see Blockers)
- [x] Full raw answer + citation list stored per check
- [x] Throttled to Gemini's current free-tier RPM/RPD caps (15 RPM / 1500 RPD), **per key slot** — fixed 2026-07-24, previously one shared bucket across all 3 keys; checks staggered ~200ms/job + jitter within a batch, not fired all at once
- [x] Retries with backoff on rate-limit errors; failed checks are requeued, never silently dropped — RPD exhaustion now throws a retryable error instead of an in-process 24h sleep (fixed 2026-07-24); a job stuck in `processing` from a crashed/timed-out worker invocation is now reclaimed by `reclaim_stale_check_jobs()` (migration 0009) at the start of every invocation
- [x] Key pool (primary/secondary/tertiary) implemented for GeminiProvider and NvidiaNimProvider per docs/CONVENTIONS.md — 429 soft-fails to the next key, 401/403 marks that key dead + alerts the Admin Console, never a silent drop. The DB-persisted dead-key set (`ai_provider_key_health`) is now actually threaded into key selection via `knownDeadSlots` (fixed 2026-07-24 — it was fetched and then discarded before)
- [x] Falls back to the standard retry/requeue path if every key in a provider's pool is dead or exhausted
- [x] Pool supports failoverMode "shared" (default; secondary/tertiary absorb 429s) vs "emergency-only" (secondary/tertiary called only on hard failures — 401/403/5xx/timeout, never a plain 429); switching to emergency-only is a one-flag change, not a redeploy of logic
- [ ] Admin Console (5.10) surfaces which failoverMode is active per provider and reminds the admin to flip it the moment primary is upgraded (billing enabled for Gemini, or the free 200 RPM increase for NVIDIA NIM) — genuinely out of scope for this module, belongs to 5.10
- [x] Free-plan workspaces never enter the recurring scheduler — checks are triggered on-demand by the user only, gated by `workspaces.experiments_used` (lifetime cap: 3, never resets); a 4th attempt is blocked outright with an upgrade prompt, never silently queued or allowed
- [x] An on-demand free-tier check runs through the exact same AIProvider / resolveTaskModel / key-pool path as a paid check — no separate cheaper code path, since it is the same Gemini grounded call either way. Note: `enqueueFreeCheck()` exists in `app/src/modules/engine-query/engine-query.ts` and is exported from the module barrel, but no UI/server action calls it yet — wiring an actual "run my free check" button is 5.6's scope (Dashboard/Frontend), not this module's

## Tech
Supabase Edge Functions + pg_cron/pg_net (see decisions log, 2026-07-23 — Railway/Fly.io's free tiers are gone as of this date, so this is no longer an either/or; Edge Functions is the committed approach).

## AI usage
Google Gemini API, Grounding with Google Search.

## Caching
Paid plans: guard against redundant calls by verifying last_checked_at for that (brand, prompt) pair is older than the plan's check interval before enqueueing — a Postgres timestamp check, not a separate cache service. Free plan: no time-based guard applies (there is no recurring cadence) — instead gated by the `workspaces.experiments_used` lifetime counter, checked before allowing an on-demand run.

## Security
Gemini key is server-only, called exclusively from this worker, never from a client-reachable route. Log request counts, never prompt/answer content, in any cross-tenant-visible log.

## Files touched
 - `app/supabase/migrations/0007_engine_query_engine.sql`, `0008_engine_query_indexes.sql` — live queue, result/config/telemetry schema, RLS and indexes
 - `app/supabase/migrations/0009_engine_query_hardening.sql` — `p_provider` param on `retry_or_fail_check_job`; new `reclaim_stale_check_jobs()` for crashed/timed-out job recovery
 - `app/supabase/migrations/0010_engine_worker_cron.sql` — enables `pg_cron`/`pg_net`, schedules `engine-worker-invoke` every 5 minutes; deliberately does not embed the worker secret (see Security below)
 - `app/supabase/migrations/0011_drop_legacy_retry_overload.sql` — drops the stale 3-arg `retry_or_fail_check_job` overload left behind when 0009 added `p_provider` (Postgres treated the new arg list as a second overload, not a replacement — found live during deploy verification, not by reading 0009's SQL alone)
 - `app/supabase/migrations/0012_update_grounded_search_model.sql` — updates the dead `gemini-2.5-flash-lite` seed to `gemini-3.5-flash-lite`, found live during the smoke test
 - `app/supabase/functions/engine-worker/index.ts` — secret-protected worker source; deployed live (function `engine-worker`, version 1, status ACTIVE, `verify_jwt: false`)
 - `app/supabase/functions/_shared/{key-pool,gemini-provider,nvidia-nim-provider,rate-limiter,task-model,schemas,deno.d}.ts` — Deno-side twins of the app-side provider code (deployed alongside the worker)
 - `app/src/lib/ai-providers/{key-pool,gemini-provider,nvidia-nim-provider,schemas,task-model,index}.ts` — Node-test-runner-covered twin of the above; not itself deployed, exists for test coverage of the same logic
 - `app/src/modules/engine-query/{engine-query,schemas,index}.ts`
 - `app/src/types/database.ts` — regenerated live via `generate_typescript_types` after migrations 0007-0011 (adds `ai_task_configs`, `ai_provider_key_health`, `ai_provider_settings`; reflects the single surviving `retry_or_fail_check_job` overload)
 - `app/tests/unit/engine-query-provider.test.ts`
 - `app/.env.example` (adds `ENGINE_WORKER_SECRET` placeholder), `app/tsconfig.json` (excludes `supabase/functions` from the Node/Next tsc project — Deno-flavored esm.sh imports aren't resolvable by it), `app/package.json` (test script now runs `node --conditions=react-server --test`, see decisions log), `.gitignore`/`app/.prettierignore` (Supabase CLI `.temp/` dirs)
 - `AGENTS.md` — Codex-tool mirror of `CLAUDE.md`, kept in sync (see decisions log)

## Decisions log
_(append-only — date, what was decided, why. Do not delete old entries even if superseded; strike through instead.)_

- **2026-07-23** — Split "swap AI vendor" from "swap model per task" into two layers: the `AIProvider` interface (vendor-level: Gemini/OpenAI/Perplexity/Copilot/NVIDIA NIM) and a new task-level config (which provider+model handles `grounded_search`, `extraction`, `explanation_generation`, `prompt_suggestion`, `outreach_email_drafting`). ~~Kept as a static file, not a DB table — no live-switching UI until there's an actual need for one.~~
- **2026-07-23 (same day, superseding the line above)** — Moved the task-level config from a static code file to a DB table (`ai_task_configs`) instead, because the actual requirement is an Admin Console UI that can enable/disable models per flow globally *and* set per-workspace overrides for custom-plan customers running model experiments — both need runtime editability a code file can't give without a redeploy. Provider API keys are unaffected by this change and stay in env vars only; the DB table stores model identifiers, never credentials. See `docs/CONVENTIONS.md` Section 5 and the entity-relationship diagram for the full design (resolution order, admin-only RLS, in-app caching with invalidation on write).
- **2026-07-23 — refinement: two failover modes, tied to the paid-tier upgrade trigger.** Proposed: once primary's account is upgraded (paid for Gemini, free RPM increase for NVIDIA NIM), it absorbs nearly all traffic, so secondary/tertiary (the teammate-owned accounts) should stop being used for ordinary rate-limit relief and become hard-failure-only fallback. This meaningfully reduces — but does not fully zero out — the risk accepted in the entry below, since the teammate keys remain wired in as emergency fallback. Target end state: once there's paying revenue, replace the teammate-owned secondary/tertiary with a second key the founder controls, fully retiring the third-party risk rather than carrying it indefinitely at a reduced rate. See docs/CONVENTIONS.md for the two-mode design.
- **2026-07-23 — Worker hosting: committed to Supabase Edge Functions + pg_cron/pg_net, dropping Railway/Fly.io as options.** Researched current (2026) free-tier status while answering a hosting question for Module 0.0: Railway no longer has a permanent free tier (only a 30-day $5 trial credit, then services pause); Fly.io no longer offers a free tier to new users either ($5/mo credit only). Both were the original candidates in this module's Tech line. Supabase Edge Functions remain genuinely free with a 150-second request timeout on the free tier — comfortably covers a grounded-search-plus-extraction chain per prompt — and require no new vendor account since Supabase is already the project's DB/auth provider. Scheduling via `pg_cron` invoking the function through `pg_net`, per Supabase's documented pattern.
  **Related, flagged for when 5.2 (prompt-suggestion) and 5.11 (free-check) are built:** Vercel Hobby's serverless function timeout is 10 seconds, hard-killed with a 504 past that. Anything in a Next.js API route that makes a *synchronous* AI call directly (the 5.2 "one Gemini call proposes 20-30 prompts" flow, and 5.11's public free-check) should budget for this — a single grounded-search call is typically a few seconds and likely fine, but should be measured once built, with the fallback of routing that specific call through a Supabase Edge Function (150s budget) instead of a Vercel API route if it ever gets close to 10s. Not a blocker now, nothing to implement yet — just don't assume unlimited synchronous request time on Vercel's free tier when 5.2/5.11 are built.
- **2026-07-23 — Free plan redesigned: 3 lifetime on-demand experiments instead of a recurring weekly cadence.** Previously the free tier ran the same recurring scheduler as paid, just weekly instead of daily — this left free-user AI quota consumption open-ended for as long as an account stayed signed up, stacking on top of the multi-key pool risk accepted below. Changed to: a Free-plan workspace gets exactly 3 lifetime on-demand checks (user-triggered, not scheduled), tracked by `workspaces.experiments_used` (see ERD) and gated here rather than left to the UI. After the 3rd run, 5.5 generates one aggregate report from the 3 data points and no further checks are allowed; 5.6 shows the existing report read-only plus an upgrade CTA. Paid plans are unaffected and keep the full recurring-scheduler behavior above. See `progress/modules/5.9-billing-and-subscription.md` decisions log for the complete before/after reasoning, and 5.5/5.6 for the corresponding Explanation-Engine and dashboard gating.
- **2026-07-25 — Independent audit of the Codex/NVIDIA-authored implementation, requested by user after that process was taking too long and was stopped early.** User forwarded a self-report ("Module 5.3 Implementation Status Summary") claiming ~80% complete with specific details (idempotency via worker_id+idempotency_key, 0-30s staggering, a `check_results` table, exponential backoff with max 3 retries, "all 30 unit tests pass cleanly"). Verified independently via direct file reads, live DB queries (`list_tables`, `execute_sql` against `information_schema`), and actually running `tsc`/`prettier`/`node --test` rather than trusting the report. Findings: the self-report was materially inaccurate on several specifics — no `worker_id`/`idempotency_key` params exist anywhere; actual max stagger is ~1.9s (200ms × 9 + jitter), not 0-30s; the actual result table is `check_runs`, not `check_results`; there is no exponential backoff, retry cap is 5 flat attempts; the test suite did not pass cleanly — 26/27 tests passed with one file (`engine-query-provider.test.ts`) crashing the whole process outright before any of its tests could run. Independently found ~10 real, evidence-based bugs beyond what the self-report even claimed as gaps (see the 2026-07-24 entries below for each). Verdict given to the user: the architecture and most of the code is sound (this recommendation, not a rewrite, is what shipped) — the failures were specific, fixable defects, not a wholesale quality problem. User chose "fix the bugs, then deploy" over a rewrite.
- **2026-07-24 (bug 1/10) — `AiProviderError` used TS constructor parameter-property shorthand, which crashed Node's test runner outright** (`ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` — parameter properties need real code generation, not just type-annotation stripping, and this project's chosen test runner only strips). Fixed by rewriting to explicit field declarations + a plain constructor body, in both the app-side and Deno-side copies.
- **2026-07-24 (bug 2/10) — the DB-persisted dead-key set (`ai_provider_key_health`, read via `getKeyHealth()` in the worker) was fetched and then silently discarded.** `withKeyFailover` kept its own in-memory-only `Set`, so a key an earlier invocation (or an admin) had marked dead got retried on every fresh cold start. Fixed by adding a `knownDeadSlots` parameter threaded from the worker's DB read all the way into `candidates()`'s filter.
- **2026-07-24 (bug 3/10) — the Gemini rate limiter used a single shared token bucket across all 3 pooled keys**, capping real throughput at what one key alone gets and defeating the entire point of the multi-key pool. Fixed: `rate-limiter.ts` now keys its `Map` by `KeySlot`, giving each of primary/secondary/tertiary an independent 15 RPM/1500 RPD bucket.
- **2026-07-24 (bug 4/10) — RPD (daily cap) exhaustion triggered an in-process `setTimeout` sleep of up to 24 hours.** An Edge Function's execution timeout would just kill the invocation mid-sleep, never actually waiting it out or returning control. Fixed to throw a normal retryable `AiProviderError("rate_limited", ...)` instead, so the job re-enters the standard retry/requeue path.
- **2026-07-24 (bug 5/10) — nothing recovered a job left stuck in `'processing'`** if the worker crashed or hit its execution timeout mid-batch — `claim_check_jobs()` flips claimed jobs to `'processing'` up front, so anything the worker didn't reach in that invocation sat there forever, directly contradicting this module's own "never silently dropped" acceptance criterion. Fixed via migration `0009`'s new `reclaim_stale_check_jobs(p_stale_after_minutes default 5)`, called at the start of every worker invocation before claiming new work.
- **2026-07-24 (bug 6/10) — `retry_or_fail_check_job` hardcoded `provider = 'gemini'`** in its `check_runs`/`engine_error_logs` inserts regardless of which provider actually failed, so an NVIDIA NIM failure would be misattributed. Fixed via migration `0009` adding a `p_provider` parameter, threaded from the worker's already-known `task.provider`. (See the 2026-07-25 entry below and migration `0011` for a follow-up: this created a second function overload rather than replacing the first, since the argument list changed — the stale 3-arg version, with the original hardcoded-`'gemini'` body, was still live and callable until `0011` dropped it. Found live in the database while verifying the deploy, not by reading `0009`'s SQL alone.)
- **2026-07-24 (bug 7/10) — the Deno edge-function twins of the provider files had no zod validation on AI-model JSON output**, contradicting `docs/CONVENTIONS.md`'s Definition of Done ("All external input ... validated with a zod schema before use"). The app-side `gemini-provider.ts` already validated with zod; its Deno twin, `nvidia-nim-provider.ts` (both copies), and `task-model.ts`'s DB-row parsing (an unchecked `as` cast) did not. Fixed by adding `app/supabase/functions/_shared/schemas.ts` (mirroring the app-side one) and wiring `groundedResponseSchema`/`nvidiaChatCompletionSchema`/a new inline `resolvedTaskModelSchema` into all of them.
- **2026-07-24 (bug 8/10) — `onKeyDead` had drifted to a different signature between the two "identical" `key-pool.ts` copies** (2-arg in the app-side file, 3-arg in the Deno file) — concrete evidence that the "keep both copies in logical sync" discipline this module's own comments call for was not actually being followed. Standardized both to `(provider, slot, code)`.
- **2026-07-24 (bug 9/10) — the worker compared `ENGINE_WORKER_SECRET` with plain `!==`**, which leaks how many leading characters matched via response-timing differences. Added a `timingSafeEqual()` helper and used it for the secret check, matching the constant-time-comparison standard this project already applies to 5.9's webhook signatures.
- **2026-07-24 (bug 10/10) — the worker's `runProviderCall()` wrapped the provider call in its own outer `withKeyFailover`, on top of the fact that `runGeminiGroundedPrompt`/`runNvidiaNimPrompt` already run their own complete failover loop internally.** The outer wrapper's key selection was completely discarded — worst case, up to 3×3=9 real HTTP attempts per job instead of 3, and the outer key was dead weight. Fixed by removing the redundant outer wrapper; the worker now calls the provider functions directly, correctly passing `knownDeadSlots` into their own internal failover.
- **2026-07-24 — re-verification after all 10 fixes:** `tsc --noEmit` clean, `prettier --check .` clean, full test suite genuinely 30/30 passing (fixed a stray extensionless-import regression along the way — Node's test runner needs explicit `.ts` suffixes on relative imports the way `tsconfig.json`'s `allowImportingTsExtensions`/`moduleResolution: "bundler"` already permits for Next.js/Turbopack; and discovered/verified `node --conditions=react-server` correctly resolves the `server-only` package's no-op branch under plain Node, permanently added to `package.json`'s `test`/`test:watch` scripts). `npm audit` after removing the unused `tsx` devDependency shows 12 pre-existing high-severity findings, all in transitive devDependencies (eslint's `minimatch` chain, Next.js's bundled `postcss`/`sharp`) — not introduced by this session; `npm audit fix --force` would downgrade Next.js to 9.3.3, clearly worse, so deliberately not auto-applied.
- **2026-07-25 — Live deployment.** Applied migration `0009` (already drafted, now actually applied live). Inserted the real `engine_worker_secret` value into Supabase Vault via a one-off `execute_sql` call (`vault.create_secret`), deliberately not via `apply_migration` — `apply_migration`'s SQL text is retained permanently in `supabase_migrations.schema_migrations` in plaintext, so a real secret has no business being embedded there; migration `0010` (already drafted, applied live) only references the Vault secret by name. Deployed the `engine-worker` Edge Function (all 8 files: `index.ts` + 7 `_shared/*.ts`) via `deploy_edge_function` with `verify_jwt: false` — matches the function's own self-authentication (a secret header, not a Supabase session JWT) and its own deploy-instructions comment. Live as function `engine-worker`, version 1, status `ACTIVE`. While verifying, found and fixed a real (11th) bug: migration `0009`'s `retry_or_fail_check_job` change added a `p_provider` parameter, which Postgres treats as a distinct overload rather than a replacement of the 3-arg version — the old 3-arg function, with the exact hardcoded-`'gemini'` bug `0009` was fixing, was still live and callable. Wrote and applied migration `0011` to drop it; confirmed via `generate_typescript_types` and a direct `pg_proc` query that only one `retry_or_fail_check_job` overload now exists. Regenerated `app/src/types/database.ts` from the live schema (adds `ai_task_configs`/`ai_provider_key_health`/`ai_provider_settings`) rather than hand-editing it, per this project's established "regenerate, don't hand-edit table shapes" rule.
- **2026-07-25 — Live smoke test found 2 more real bugs, both fixed and re-verified against the real Gemini API (not mocks).** (11/12, 12/12 — continuing the numbering from the 10 found in the earlier audit.) **Bug 11:** `engine-worker/index.ts`'s `rpc()` helper called `await response.json()` unconditionally. PostgREST returns `204 No Content` (empty body) for `returns void` functions (`retry_or_fail_check_job`, `complete_check_job`) unless a `Prefer: return=representation` header is sent, which this helper never sent — parsing an empty body threw inside the per-job `catch`, escaping it and turning an already-handled provider failure into an unhandled 500 for the whole invocation (the DB write itself succeeded; only the worker's own HTTP response was wrong). Fixed by treating `204`/empty bodies as success-with-no-payload. **Bug 12 (data, not code):** `ai_task_configs.grounded_search`'s seeded model, `gemini-2.5-flash-lite`, is dead — Google deprecated the 2.5 family ahead of its stated shutdown date; confirmed live via a real `404 "no longer available to new users"` response through this exact worker. Updated to `gemini-3.5-flash-lite` (current GA model, same `generateContent` REST endpoint, cheapest in the 3.5 family — fits this project's free-tier-first design) via migration `0012`. After both fixes, live-verified: worker auth succeeds, RPC round-trips succeed, the new model resolves and reaches Gemini for real, and the retry/backoff path was exercised against a genuine `429` rate-limit response from Google (not mocked) — `attempts` incremented, `last_error_code` recorded correctly, job re-queued via `retry_or_fail_check_job` exactly as designed. A fully successful `check_runs` row (past the rate limit caused by this session's own repeated test traffic) is expected on the next `pg_cron` tick once quota recovers — not yet manually re-confirmed as of this entry, left queued rather than force-retried further to avoid compounding the rate limit.
- **2026-07-25 — Committed all of the above** (migrations 0007-0011, both app-side and Deno-side provider code, the test file, `package.json`/`.gitignore`/`.prettierignore`/`tsconfig.json` housekeeping) as a single commit; see git log for the hash. Also found, while staging, that the entire 5.3 implementation had never actually been committed at all despite the self-report's "committed" implication — everything from `0007` onward was still working-tree-only. Kept `code-reviews/runs/review-run-001-module-5.3-engine-query-engine.md` per this project's append-only policy but added a correction notice at its top listing the specific inaccurate claims, rather than deleting or silently rewriting it. Left `app/delete_orphan_tmp.mjs.stale` and `AEO_antigravity.code-workspace` out of the commit — the former is a one-off completed cleanup script (Module 5.1's orphaned-account fix) with no further use, the latter is an IDE-local workspace file, now gitignored.
- **2026-07-23 — multi-key pool, risk explicitly flagged and accepted.** Decided to run each of Gemini and NVIDIA NIM behind a 3-key pool (primary/secondary/tertiary) during the free-tier period, using two additional accounts belonging to other team members alongside the founder's own. Before this was decided, the following was raised: (1) both providers enforce free-tier rate limits per account, so this only works with genuinely separate accounts, which is the exact pattern both providers' own abuse-monitoring treats as circumvention; (2) because all three keys are called from one backend for one product, detection is behavioral/pattern-based, not identity-based — it is expected to correlate and suspend all three accounts together if flagged, not one at a time, regardless of the fact that the accounts belong to real distinct people rather than fabricated identities; (3) a suspension would affect the account owner's personal use of that provider too, not just this product. The decision was made anyway, with the condition that every account owner has been told explicitly that their own account carries this risk. Scope: free-tier period only — this pool is removed once the product is funded onto a paid tier (the upgrade path already documented in spec Section 6.4). Required as a condition of this decision: dead-key detection must alert immediately (see acceptance criteria above and `docs/CONVENTIONS.md`), not fail silently.

## Blockers
- None genuinely blocking. Edge Function secrets are set (user completed this 2026-07-25); the worker is deployed, authenticates correctly, and has been proven live against the real Gemini API through the retry/rate-limit path. The one thing not yet manually re-confirmed is a fully **successful** `check_runs` row (the smoke-test job hit Google's real rate limit from this session's own repeated test calls before a clean success could land) — it's queued and will complete on the next `pg_cron` tick once quota recovers; check `check_runs`/`check_jobs` for job id `45a3eb82-e226-4780-a741-4df2940e408b` (throwaway `__smoketest_5.3_*` workspace/brand/prompt) to confirm, then delete that test data.

## Next steps
1. Confirm the smoke-test job (above) landed a real completed `check_runs` row with actual grounded text + citations; delete the throwaway `__smoketest_5.3_*` workspace/brand/prompt/job once confirmed.
2. Flip status to `in_review`, then `done` — this file's one remaining unchecked acceptance item (5.10 Admin Console UI for `failoverMode`) is genuinely out of this module's scope, not a gap in this module's own work.

_(Original scaffold next-steps below, superseded by the above — kept per this project's append-only policy.)_

0. **2026-07-24 implementation note:** Migrations `0007` and `0008` were applied to the live Supabase project. They provide task-model configuration, a durable queue claimed with `FOR UPDATE SKIP LOCKED`, raw Gemini answer/citation storage, plan-aware due guards, atomic three-check Free-plan enforcement, retry/error/key-health telemetry, and indexes. Provider adapters implement both shared and emergency-only failover modes. The Supabase security advisor's RLS-with-no-policy INFO findings are intentional for admin/worker-only tables; the one customer-visible result table has a workspace-member select policy. The `enqueue_free_check` SECURITY DEFINER warning is intentional because it needs an atomic counter update plus queue insert and performs its own caller/workspace/brand/prompt authorisation.
1. Confirm dependencies (`5.2`) are `done` in progress.json before starting.
2. Implement against the acceptance criteria above.
3. Satisfy every applicable item in the shared Definition of Done (progress/progress.json → `definitionOfDone`).
4. Update this file's Status, Files touched, and Decisions log, and mirror the status/percentComplete change in progress.json.
