# Module 5.5 — Visibility Scoring, Share-of-Voice & Explanation Engine

**Status:** in_review (implemented, independently verified, and live-deployed 2026-07-26 — one manual step left, see Blockers, before `done`)
**Phase:** 2 (see docs spec Section 10 for phase timing)
**Spec reference:** Section 5.5 of AEO_Visibility_Platform_Spec_v1.1.docx
**Depends on:** 5.4
**Blocks:** 5.8, 5.12
**Owner:** unassigned
**Last updated:** 2026-07-26 by Claude (independent verification pass)

## Purpose

Aggregates results into the Visibility Score, Share-of-Voice, and the Explanation Engine (why a competitor wins + Opportunity Finder + confidence-tagged actions) — the product's flagship differentiator.

## Acceptance criteria

- [ ] Visibility Score (0-100), raw mention count, and average rank computed and stored
- [ ] Share-of-Voice vs named competitors, trended weekly
- [ ] Source Influence report (citation domains by type) computed
- [ ] Explanation Engine: percentage breakdown of why a competitor is cited more, via one NVIDIA NIM call per brand per check cycle, model resolved via resolveTaskModel("explanation_generation", workspaceId) against ai_task_configs — not hardcoded in this module
- [ ] Opportunity Finder: gap list of cited source types the brand has zero presence on
- [ ] Recommended actions are confidence-tagged (High/Medium/Low), never stated as flat guarantees
- [ ] Explanation Engine, Opportunity Finder, and recommended actions are computed only for paid-plan workspaces (`workspaces.plan_tier != 'free'`) — gated here, not just hidden in the UI, so Free-plan traffic never triggers the NVIDIA NIM call at all
- [ ] Free-plan workspaces get Visibility Score, mention count, average rank, Share-of-Voice, and Source Influence only; after the 3rd (final) lifetime experiment, one aggregate report is generated combining all 3 runs into a 3-point trend — still without the Explanation Engine

## Tech

Postgres aggregation (SQL) for scoring; one NVIDIA NIM call per brand per cycle for the explanation text.

## AI usage

NVIDIA NIM (same model class as 5.4).

## Caching

Aggregates (score, share-of-voice, explanation) are recomputed only when a new check result lands for that brand — cache the computed result and invalidate on new data, rather than recomputing on every dashboard load.

## Security

Recommendation text is model-generated — label it as guidance in the UI, never as a guarantee, to avoid overstating certainty to customers.

## Files touched

- `app/supabase/migrations/0016_visibility_scoring_and_explanation.sql` (new)
- `app/supabase/migrations/0017_scoring_worker_cron.sql` (new)
- `app/src/modules/scoring-explanation/schemas.ts` (new)
- `app/src/modules/scoring-explanation/prompt.ts` (new)
- `app/src/modules/scoring-explanation/parse-explanation-response.ts` (new)
- `app/src/modules/scoring-explanation/index.ts` (modified — placeholder barrel filled in)
- `app/supabase/functions/_shared/scoring-explanation.ts` (new)
- `app/supabase/functions/scoring-worker/index.ts` (new)
- `app/tests/unit/scoring-explanation.test.ts` (new)
- `app/.env.example` (modified — `SCORING_WORKER_SECRET` added)
- `docs/architecture/entity-relationship-diagram.md` (modified — see 2026-07-26 entry)

All match the file list in `progress/specs/5.5-*.md` exactly; `git status` confirmed no unrelated dirty files were swept in.

## Decisions log

_(append-only — date, what was decided, why. Do not delete old entries even if superseded; strike through instead.)_

- **2026-07-23** — Explanation-generation model choice sourced from the shared `ai_task_configs` DB table (task key: `explanation_generation`), owned by Module 5.3, instead of being decided locally. See `docs/CONVENTIONS.md` Section 5.
- **2026-07-23** — Explanation Engine/Opportunity Finder/recommended actions gated to paid plans only, computed here (not just hidden downstream in 5.6) so Free-plan traffic never costs an NVIDIA NIM call. Part of the wider Free-plan redesign — see `progress/modules/5.9-billing-and-subscription.md` decisions log.
- **2026-07-25** — Full implementation spec written: `progress/specs/5.5-visibility-scoring-share-of-voice-and-explanation-engine.md`. Confirmed exact requirements directly against the source docx's Section 5.5 (extracted via `python-docx`, not this file's condensed summary), plus Section 3.5 (Trust & Transparency Design Principle) and Section 4 (System Architecture Overview) for supporting context. **This module has no frontend component** — confirmed via the docx's own "Tech" line ("Pure SQL/backend aggregation for scoring and share-of-voice; one NVIDIA NIM call per brand per check cycle..."); Module 5.6 (Dashboard/Frontend) owns 100% of the UI/visual layer that will eventually display this module's output.
  Key architecture calls (full reasoning in the spec file):
  1. Unlike 5.3/5.4's "enqueue empty row, worker fills everything" pattern, a `visibility_snapshots` row is inserted **already containing** its real score/mention_count/avg_rank/share_of_voice/source_influence/opportunity_gaps — pure SQL, zero external dependency, computed synchronously. The status/queue/retry machinery applies **only** to the optional, paid-only explanation-prose sub-step.
  2. Trigger model: a periodic (every 30 min) cron scan for brands with fresh `check_extractions` data since their last snapshot, computing a rolling 7-day window — not a per-check trigger, to avoid over-scoring and to match this module's own caching note.
  3. This same freshness-check mechanism, unmodified, satisfies the Free-plan "3-point trend" acceptance criterion — no special-case code needed, since 3 sparse lifetime experiments naturally produce exactly 3 snapshot rows over time.
  4. The Explanation Engine's NVIDIA NIM call is restricted to **prose + confidence-tagged actions only, never a number** — a direct response to Module 5.4's live smoke test (2026-07-25), which found this model class doesn't reliably self-check literal facts even when explicitly instructed to. Every number here (score, percentages, the opportunity-gap list, the "winning competitor" determination) is deterministic SQL, computed before the model ever sees it.
  5. Extends `docs/architecture/entity-relationship-diagram.md`'s documented `visibility_snapshots` shape with `workspace_id`, `source_influence` (new — the ER diagram's current column list has no place for this distinct acceptance criterion), `explanation_skip_reason`, and the explanation-sub-step's queue/retry columns; the spec requires updating that ERD doc as part of the deliverable.

  The spec has a dedicated "lessons from 5.4's delegation" section addressing every real gap independent verification found in that module (hand-edited `database.ts`, an invented type, a false `.env.example` claim, unrelated files swept into a commit, unverified live-step claims) as explicit do-not-repeat items for whichever model implements this one. Status/percentComplete intentionally left unchanged (`not_started`/0) — a spec is not an implementation.

- **2026-07-26** — Delegate model (external, via `delegate-and-verify`) implemented against the spec; independently verified against ground truth rather than trusting the self-report. Findings:
  - **Confirmed true:** migration 0016 matches the spec's provided SQL byte-for-byte (table is `visibility_snapshots`, not the `visibility_explanations` name used in the delegate's own prose summary — the file itself is correct, only the delegate's report text was sloppy about naming it). `run_visibility_scoring_cycle()`, the 4 explanation-queue RPCs, RLS policy, and indexes are all present and match spec. Migration 0017's cron cadence (`*/30 * * * *`) and naming match spec exactly. `app/supabase/functions/_shared/scoring-explanation.ts` is a faithful byte-for-byte Deno mirror of the three app-side module files — no drift. The `DOMAIN_TYPES` literal list in the migration (`review_site`, `comparison_page`, `forum`, `documentation`, `other`) matches `app/src/modules/nlp-extraction/schemas.ts` exactly — confirmed by direct read, not delegate's say-so. `tsc --noEmit`, `eslint`, and `prettier --check` all ran clean on every new/changed file. No hand-edit of `database.ts`, no scope creep — `git status` shows only the spec's listed files touched.
  - **Real gap, fixed directly (small, mechanical):** the spec required updating `docs/architecture/entity-relationship-diagram.md`'s `visibility_snapshots` row in the "Full column reference" table with the new columns (`workspace_id`, `source_influence`, `explanation_skip_reason`, `status`, `attempts`, `claimed_at`, `last_error_code`, `explanation_provider`, `explanation_model`, `explanation_completed_at`, `recommended_actions`, `share_of_voice`) plus a design-decision sentence explaining the `source_influence` addition and the sync-numbers/async-prose split. The delegate's actual diff did neither — it only added an unrelated `BRANDS ||--o{ CHECK_EXTRACTIONS : produces` relationship line (a real, correct fix, but for a gap left over from Module 5.4, not this spec's actual documentation requirement). Fixed directly in this verification pass: added the full column list and the design-decision paragraph; left the `CHECK_EXTRACTIONS` line in place since it's independently correct.
  - **Real deviation, flagged not fixed — needs your call:** `prompt.ts`'s `buildExplanationPrompt` does not match the spec's provided reference implementation. The delegate (a) added an `explanationBreakdown` parameter (`{ competitor_name, citation_ratio, breakdown }`) not in the spec's type signature — the spec's own lesson #2 says "never invent a type/field not in this spec... flag it, don't add it unasked" — and (b) replaced the spec's qualitative confidence guidance ("high only when a zero-presence gap directly motivates it, medium when a reasonable inference, low when general best practice") with an invented numeric threshold rule ("high when `competitor_pct > 15%`, medium 5-15%, low otherwise"). Neither breaks anything — `claim_visibility_explanation_jobs` doesn't return `explanation_breakdown`, so `scoring-worker/index.ts` synthesizes it inline from fields it already has (`competitor_mention_count / brand_mention_count`, unrounded — the SQL's own precomputed, rounded-to-1-decimal `citation_ratio` in `explanation_breakdown` is never read back by the worker, a minor inconsistency, not a bug). The numeric threshold is an instruction *to* the model, not something the model computes, so it doesn't reintroduce the "don't let the model do arithmetic" risk lesson #6 warns about. Left as-is rather than rewritten back to the spec's exact wording, since it's arguably a more consistent design (gives the model a concrete calibration anchor) — but it's a real off-spec change made without flagging it, and you should decide whether to keep it or revert to the spec's literal text.
  - **Could not independently verify — pre-existing, not a regression:** running `node --test tests/unit/scoring-explanation.test.ts` (and separately, the already-`done` Module 5.4's `tests/unit/nlp-extraction.test.ts`) fails before any assertion runs, with `ERR_MODULE_NOT_FOUND` on the bare `@/lib/ai-providers/errors` import — Node's native test runner doesn't resolve the `tsconfig.json` `@/*` path alias without a loader, and no such loader (`node_modules/@` symlink, `package.json` `"imports"` map, or `NODE_OPTIONS`) exists in this checkout. `npm test` (the full suite, matching CI's exact command) shows 30/32 passing subtests, with exactly these two alias-dependent files failing outright. Since this affects 5.4's already-verified test file identically, it's almost certainly an environment/tooling gap (possibly a local `NODE_OPTIONS` env var on a prior session's machine that isn't captured in the repo) rather than a defect in this module's test content — the test file itself reads correctly and mirrors 5.4's exact structure per spec instruction. Flagging rather than silently marking "tests pass": **you should confirm `npm test` actually passes in your real dev environment / CI before trusting this DoD item**, since if it's broken there too, this predates Module 5.5 and CI may have been silently red.
  - **Explicitly unverified, requires live infrastructure (delegate correctly declined to claim these, consistent with lesson #5):** migrations 0016/0017 have not been applied to the live Supabase DB, `scoring-worker` has not been deployed as an Edge Function, no live NVIDIA NIM call has been made, and per `progress.json`'s `definitionOfDone` → `deploy` item (added after 5.2's incident), a Vercel deployment READY check is required for any module touching `app/src/modules/**` — not yet possible since nothing has been committed/pushed yet.
  - **RLS test:** per the spec's own note, no new dedicated RLS integration test was added — `visibility_snapshots`' policy is structurally identical to `check_extractions`' already-tested `is_workspace_member`/select-only pattern (confirmed by direct read of the migration). Judged adequate; not a gap.
  - Net: this module is **not** ready to mark `done`. No commits made yet (delegate correctly left this to Heisenberg per protocol). Before moving to `done`: (1) decide on the `prompt.ts` deviation above, (2) commit and push, (3) apply migrations 0016/0017 to the live DB, (4) deploy `scoring-worker` and set `SCORING_WORKER_SECRET`, (5) confirm the Vercel deployment is READY, (6) run a live smoke test (at least one real `run_visibility_scoring_cycle()` call + one real NVIDIA NIM explanation call), (7) confirm `npm test` actually passes in a real environment.

- **2026-07-26 (follow-up)** — Resolved the `prompt.ts` deviation per Heisenberg's direction to "finalize the perfect design, using future scope in mind": kept the delegate's good instinct (give the model a precomputed ratio so it never has to divide two numbers itself) but fixed how it gets there, and reverted the part that was actually risky.
  - `citationRatio` is now sourced from a single place: `run_visibility_scoring_cycle()`'s already-rounded `explanation_breakdown.citation_ratio`, plumbed straight through `claim_visibility_explanation_jobs()`'s new `citation_ratio` return column (migration 0016 edited in place — safe, since it had never been applied live) to the worker to the prompt. The worker no longer re-derives it via `competitor_mention_count / brand_mention_count`, so there's exactly one source of truth instead of two slightly different numbers.
  - Dropped the redundant `explanationBreakdown.breakdown` field, which was a verbatim duplicate of `competitorCitationProfile` passed under a second name for no reason.
  - Reverted the numeric confidence-threshold rule (`competitor_pct > 15%` / `5-15%` / else) back to the spec's original qualitative, categorical rubric ("high only when a listed zero-presence gap directly motivates the action..."). This wasn't just a style call: a threshold comparison is itself the exact class of numeric-judgment task Module 5.4's live smoke test (2026-07-25) already proved this model class (NVIDIA NIM Llama-3.1-8B-class) doesn't reliably get right — reintroducing a percentage-threshold check inside the prompt would have reintroduced the same risk lesson #6 was written to close off, just moved one level up (from "count/match" to "compare a percent to 15").
  - Updated in lockstep: `app/src/modules/scoring-explanation/prompt.ts`, `app/supabase/functions/_shared/scoring-explanation.ts` (Deno mirror, re-verified byte-identical), `app/supabase/functions/scoring-worker/index.ts` (Job type + call site), `app/tests/unit/scoring-explanation.test.ts`, and `app/supabase/migrations/0016_visibility_scoring_and_explanation.sql`.
  - **Second real bug found and fixed while re-reading this code for the redesign, not previously caught:** `parse-explanation-response.ts` defined its own local `AiProviderError` class instead of importing the project's one shared class from `@/lib/ai-providers/errors` (the exact pattern `nlp-extraction/parse-extraction-response.ts` uses). It happened to type-check and even pass tests because the test file's own `instanceof`-style check used structural duck-typing, not `instanceof` against the real shared class — a latent bug that would have caused any future `instanceof AiProviderError` check elsewhere in the codebase to silently fail on explanation-engine errors. Fixed: `parse-explanation-response.ts` now imports the shared class; `index.ts` no longer re-exports a local one (matching `nlp-extraction/index.ts`'s exact barrel shape). The Deno mirror's own local `AiProviderError` class was left untouched — that duplication is deliberate and correct there, since Deno Edge Functions can't reach `@/lib/...` (same pattern `_shared/nlp-extraction.ts` already uses).
  - **Root-caused and fixed the `@/` alias test failure — it was real, not sandbox-only, and pre-existing (predates this module).** `node --test` cannot resolve the `tsconfig.json` `@/*` path alias without a loader; nothing in this repo provided one. Fix: swapped `node` for `tsx` (already a devDependency, resolves tsconfig paths natively) in `package.json`'s `test`/`test:watch` scripts, keeping the existing `--conditions=react-server` flag (tsx forwards it straight to node) so the `server-only` guard still works. Verified `npm test` — the literal command CI runs — now passes **84/84**, up from 30/32 with 2 files crashing at import time before this fix. This also silently fixes Module 5.4's `nlp-extraction.test.ts`, which had the identical failure; CI was very likely red on every push since 5.4 shipped, unnoticed because nobody had run `npm test` locally with a clean install matching CI's node resolution. Worth flagging to Heisenberg as a real finding independent of 5.5.
  - `tsc --noEmit`, `eslint`, `prettier --check .` all re-verified clean after every edit above.

- **2026-07-26 (live infra pass)** — Continued per Heisenberg's "finish all other things": applied migration 0016 to the live Supabase project (`vloradmcvozmhvvxiyvd`) via the Supabase MCP connector.
  - **Third real bug found — this one only a live execution could catch, and it's the most serious of the three found today.** `run_visibility_scoring_cycle()` failed immediately on first live invocation: `ERROR: 42803: aggregate function calls cannot contain window function calls`. Two blocks (the `source_influence` computation and the explanation-prose `breakdown` computation) nested a window function (`sum(...) over ()`) directly inside a `jsonb_agg(...)` call's arguments — invalid in Postgres; a window function must be resolved in its own subquery level before an outer query aggregates the result. This exact bug was present in the spec's own "complete, carefully-reasoned" reference SQL (`progress/specs/5.5-*.md`), copied verbatim into migration 0016 by the delegate, and missed by every static check in the first verification pass (`tsc`, `eslint`, byte-for-byte spec diff) because none of those execute SQL — only actually running the function against a live database surfaced it. This is the concrete case for why `delegate-and-verify`'s step 4 insists on hitting live systems for real rather than stopping at static checks. Fixed by restructuring both blocks so the window function resolves in an inner subquery first, then an outer query aggregates the resulting plain column (the same pattern `claim_visibility_explanation_jobs`'s citation-profile aggregation already used correctly, which is why that RPC didn't have this bug). Fixed in the migration 0016 source file (safe, matches what's live) and hotfixed live via a direct `create or replace function` (not a new migration file/ledger entry, since 0016 hadn't shipped to anyone else yet — the source file and the live DB now match).
  - **Live smoke test, real evidence:** `select run_visibility_scoring_cycle()` → `1` snapshot created. Inspected the actual row: `score: 100, mention_count: 1, avg_rank: 1.0, share_of_voice: {brand: {name: "Acme Corp", mention_count: 1, share_pct: 100}, competitors: []}, source_influence: [], status: "not_applicable", explanation_skip_reason: "free_plan"` — correct given the real underlying data (this test workspace has 1 completed `check_extractions` row, brand mentioned at rank 1, 0 rows in `competitors`, and is on the Free plan, so the DCG score of 100 and the `free_plan` gate are both exactly right, not just "didn't error"). `claim_visibility_explanation_jobs(10)` and `reclaim_stale_visibility_explanations(5)` both ran cleanly (correctly returned empty/0, since nothing is queued on this Free-plan-only test data). **Not yet exercised live: the paid-plan, competitor-ahead branch** (the one that populates `opportunity_gaps`, `explanation_breakdown`, and sets `status = 'queued'`) — no paid workspace with a named, higher-mention-count competitor exists in current data. Recommend testing this branch once real customer data exists, or ask me to construct a disposable test scenario if you want it verified sooner.
  - **Deployed `scoring-worker` as a live Edge Function** (id `8c2f3409-f370-492a-b0ff-7b694304d847`, status `ACTIVE`, `verify_jwt: false` matching `extraction-worker`'s pattern), with its full dependency set (`_shared/key-pool.ts`, `_shared/task-model.ts`, `_shared/nvidia-nim-provider.ts`, `_shared/schemas.ts`, `_shared/scoring-explanation.ts`) — but it cannot be invoked for real yet; see Blockers.
  - Migration 0017 (the pg_cron schedule) and setting `SCORING_WORKER_SECRET` were **not completed in this pass** — resolved in the follow-up entry immediately below.

- **2026-07-26 (finish-out pass)** — Resolved all three blockers from the entry above, per Heisenberg's direction.
  - **Git access:** the repo's own `.git/config` (`core.sshCommand`) and `.git/ssh_config` (`IdentityFile`) both still pointed at a *previous* sandbox session's mount path (`/sessions/dazzling-eloquent-thompson/...`) — the deploy key itself (`.git/deploy_key`) was present and fine, just referenced by a now-wrong absolute path, exactly the same class of issue Module 5.4's decisions log already documents fixing once before (each new Cowork session gets a differently-named sandbox mount path, so this breaks again every session unless something more permanent is set up). Fixed both paths to this session's actual mount. Separately hit a `.git/index.lock` that `rm`/`unlink` couldn't remove (`Operation not permitted`) even though it was safe to clear — `mv`-ing it aside worked (confirmed via `.git/index.lock.stale*` that many prior sessions hit and worked around the same FUSE-mount quirk this same way). Committed (`c24b8ed`, 14 files) and pushed to `origin/main` successfully.
  - **Migration 0017:** retried per Heisenberg's choice and it applied cleanly this time (no classifier block on the retry). While reviewing it against migration 0014 (the already-working, `done` extraction-worker cron) to sanity-check before trusting it, found a fourth real bug: the spec's reference SQL used `vault.decrypt('scoring_worker_secret')`, which is not a real Supabase Vault function — copied verbatim into 0017 and would have made the cron job fail its `net.http_post` call (or fail to parse) on every single tick, silently, forever. 0014's actual, proven pattern reads `(select decrypted_secret from vault.decrypted_secrets where name = '...')` instead. Fixed in the migration source and re-applied live via `cron.schedule` (idempotent by job name — replaces the broken definition, no duplicate job created). Verified via `select * from cron.job where jobname = 'scoring-worker-invoke'`: correct command text, `active = true`.
  - **`SCORING_WORKER_SECRET`:** generated a random 32-byte value and stored it in Supabase Vault as `scoring_worker_secret` via `vault.create_secret(...)` (confirmed readable via `vault.decrypted_secrets`). Per Heisenberg's own acknowledgment when choosing this option, this only covers the Vault half the cron job reads — no available MCP tool can set the Edge Function's own `Deno.env` secret, so `scoring-worker` will still 401 on every real invocation until that's set to the same value via the Supabase dashboard (Edge Functions → scoring-worker → Secrets) or `supabase secrets set`. The value was shared once, directly to Heisenberg, for exactly this purpose — never committed to any file.
  - **Vercel deploy check:** confirmed via the Vercel MCP connector, not assumed. Deployment `dpl_GkgV85ykEibXtNkr1B3ZdebYeZC8` for commit `c24b8ed` (this module's commit) — project `aeo`, target `production` — is `READY`.
  - Net: everything in this module's scope that doesn't require typing a secret into the Supabase dashboard is now done and live-verified. The one remaining step is entirely on Heisenberg's side (paste the shared secret value into the dashboard) — once done, `scoring-worker` should start actually processing on its 30-minute cron tick for any paid-plan brand with a competitor ahead of it.

## Blockers

- **Set `SCORING_WORKER_SECRET` as the `scoring-worker` Edge Function's own secret** (Supabase dashboard → Edge Functions → scoring-worker → Secrets, or `supabase secrets set SCORING_WORKER_SECRET=<value>`), using the exact value Claude generated and shared directly in the 2026-07-26 finish-out session (also stored in Vault as `scoring_worker_secret` already — the two must match). This is the only step left that no available tool can do.
- The paid-plan/competitor-ahead branch of `run_visibility_scoring_cycle()` (the one that populates `opportunity_gaps`/`explanation_breakdown` and queues an explanation job) has not been exercised live — no matching test data exists yet (current test workspace is Free-plan, 0 competitors). Worth a deliberate test once the secret above is set, or once real customer data exists.

## Next steps

1. ~~Hand `progress/specs/5.5-visibility-scoring-share-of-voice-and-explanation-engine.md` to a delegate model via the `delegate-and-verify` skill's executor prompt.~~ Done 2026-07-26.
2. ~~Independently verify (per `delegate-and-verify`) — do not trust the delegate's self-report.~~ Done 2026-07-26 — see decisions log.
3. ~~Resolve the `prompt.ts` deviation.~~ Done 2026-07-26 — finalized design, see follow-up decisions log entry.
4. ~~Confirm `npm test` passes for real.~~ Done 2026-07-26 — root-caused and fixed (see decisions log); 84/84 passing via `npm test`.
5. ~~Apply migration 0016 live and run a live smoke test.~~ Done 2026-07-26 — found and fixed a real Postgres bug in the process (window-function-inside-aggregate), confirmed a correct real snapshot row. See decisions log.
6. ~~Deploy `scoring-worker`.~~ Done 2026-07-26 — live, `ACTIVE`, id `8c2f3409-f370-492a-b0ff-7b694304d847`.
7. ~~Apply migration 0017 (cron).~~ Done 2026-07-26 — also found and fixed a real bug (`vault.decrypt(...)` isn't a real function; switched to the proven `vault.decrypted_secrets` pattern from migration 0014). Confirmed live: job active, correct command text.
8. ~~Generate and set `SCORING_WORKER_SECRET` in Vault.~~ Done 2026-07-26. Still needed: the *Edge Function's own* secret (see step 9) — Vault alone doesn't make `scoring-worker` accept requests.
9. **Set the `scoring-worker` Edge Function's `SCORING_WORKER_SECRET` via the Supabase dashboard or CLI**, using the value Claude shared directly in this session — the only step left, and the only one no available tool can do.
10. ~~Commit and push.~~ Done 2026-07-26 — fixed a stale SSH config path (same class of issue as 5.4's prior fix, recurs each new sandbox session) and a filesystem lock quirk; pushed `c24b8ed`.
11. ~~Confirm Vercel deployment READY.~~ Done 2026-07-26 — `dpl_GkgV85ykEibXtNkr1B3ZdebYeZC8`, commit `c24b8ed`, target `production`, state `READY`.
12. Once the Edge Function secret (step 9) is set: invoke `scoring-worker` for real and confirm a real NVIDIA NIM explanation gets written — needs a paid-plan workspace with a named competitor ahead of the brand (none exists in current data; may need a deliberately constructed test scenario).
13. Only then flip status to `done` in this file and in `progress/progress.json`, and check off the acceptance criteria above with real evidence.
