# Module 5.6 — Dashboard / Frontend

**Status:** done (100%) — see 2026-07-26 entries below for the real verification/fix/push/deploy trail behind this; see 2026-09-01 entries for the design-system-coverage fix (auth/landing/brand-creation/admin restyle)
**Phase:** 1-3 (see docs spec Section 10 for phase timing)
**Spec reference:** Section 5.6 of AEO_Visibility_Platform_Spec_v1.1.docx
**Depends on:** 5.3 (tracker-formal) — real data also needs 5.4/5.5, see decisions log
**Blocks:** none
**Owner:** unassigned
**Last updated:** 2026-09-01 by Claude (restyled every remaining non-design-system UI surface -- auth pages, root landing page, brand creation, and the full admin console -- onto the Module 5.6 token/component system; committed, pushed, confirmed CI green + Vercel READY)

## Purpose

Customer-facing app: Overview, Prompt Explorer, Competitor Explorer, Reports, Settings — five views, not one monolithic page.

## Acceptance criteria

- [x] Overview: Visibility Score, raw mention count, average rank, Share-of-Voice trend; Free-plan workspaces additionally show remaining experiments (e.g. "2 of 3 free checks used")
- [x] Prompt Explorer: per-prompt drill-down + browsable AI Response History timeline
- [x] Competitor Explorer: Share-of-Voice always shown; Explanation Engine + Opportunity Finder panels render as a visible locked/upsell state for Free-plan workspaces (never silently omitted), full panels for paid
- [x] Reports: crawl-readiness checklist + exportable/white-label summaries (Agency plan)
- [x] Settings: brand/prompt config, team, billing portal link
- [x] Every result explicitly labeled by engine ("Gemini Visibility") with a one-click "How we calculated this" expansion
- [x] Free-plan workspaces past the 3-experiment cap: "run a new check" replaced with an upgrade CTA; all previously generated report data remains fully viewable — read access is never revoked

## Tech

Next.js + TypeScript + Tailwind, deployed on Vercel.

## AI usage

None directly — renders data other modules computed.

## Caching

Use Next.js data cache with tag-based revalidation, invalidated when a new check result lands for that brand — avoids recomputing/refetching on every page view between check cycles.

## Security

CSRF protection on forms; all AI-sourced text (answers, explanations) rendered via safe text components, never dangerouslySetInnerHTML — it is untrusted third-party content.

## Files touched

```
app/src/app/(dashboard)/layout.tsx
app/src/app/(dashboard)/overview/page.tsx
app/src/app/(dashboard)/prompts/page.tsx
app/src/app/(dashboard)/prompts/[promptId]/page.tsx
app/src/app/(dashboard)/competitors/page.tsx
app/src/app/(dashboard)/reports/page.tsx
app/src/app/(dashboard)/settings/page.tsx
app/src/app/(dashboard)/settings/brands/[brandId]/page.tsx
app/src/app/dashboard/page.tsx                          (replaced with a redirect to /overview)

app/src/components/ui/card.tsx
app/src/components/ui/metric-stat.tsx
app/src/components/ui/engine-badge.tsx
app/src/components/ui/plan-badge.tsx
app/src/components/ui/calculation-disclosure.tsx
app/src/components/ui/drawer.tsx
app/src/components/ui/locked-panel.tsx
app/src/components/ui/empty-state.tsx
app/src/components/ui/data-table.tsx
app/src/components/ui/tabs.tsx
app/src/components/ui/theme-toggle.tsx

app/src/modules/dashboard/queries.ts
app/src/modules/dashboard/types.ts
app/src/modules/dashboard/index.ts
app/src/modules/dashboard/cache.ts

app/src/app/globals.css                    (modified — full design-system token replacement)
app/src/app/layout.tsx                     (modified — confirm/wire Geist Sans+Mono, theme-cookie read)

docs/CONVENTIONS.md                        (modified — add the app/src/components/ folder to Section 1)

app/src/app/(dashboard)/header.tsx                 (modified 2026-09-01 — fixed stale plan-badge bug)
app/src/app/(dashboard)/navigation.tsx             (modified 2026-09-01 — /help link)
app/src/app/(dashboard)/settings/page.tsx          (modified 2026-09-01)
app/src/app/(dashboard)/settings-view.tsx          (modified 2026-09-01 — billing payments-on-hold state)
app/src/app/(dashboard)/prompts/page.tsx           (modified 2026-09-01 — isAdmin wiring)
app/src/app/(dashboard)/prompt-explorer-view.tsx   (modified 2026-09-01 — admin run-check-now + polling)
app/src/app/(dashboard)/competitor-explorer-view.tsx (modified 2026-09-01)
app/src/components/ui/skeleton.tsx                 (new 2026-09-01)
app/src/app/(dashboard)/overview/loading.tsx        (new 2026-09-01)
app/src/app/(dashboard)/prompts/loading.tsx         (new 2026-09-01)
app/src/app/(dashboard)/competitors/loading.tsx     (new 2026-09-01)
app/src/app/(dashboard)/reports/loading.tsx         (new 2026-09-01)
app/src/app/(dashboard)/settings/loading.tsx        (new 2026-09-01)
app/src/app/(dashboard)/help/page.tsx               (new 2026-09-01)
app/src/app/(dashboard)/help/loading.tsx            (new 2026-09-01)
app/src/app/page.tsx                                (restyled 2026-09-01 -- root landing page onto design system)
app/src/app/(auth)/layout.tsx                       (restyled 2026-09-01)
app/src/app/(auth)/sign-in/page.tsx                 (restyled 2026-09-01)
app/src/app/(auth)/sign-up/page.tsx                 (restyled 2026-09-01)
app/src/app/brands/new/page.tsx                     (restyled 2026-09-01)
app/src/app/brands/new/brand-form.tsx               (restyled 2026-09-01)
app/src/app/admin/page.tsx                          (restyled 2026-09-01)
app/src/app/admin/loading.tsx                       (restyled 2026-09-01)
app/src/app/admin/plan-tier-section.tsx             (restyled 2026-09-01)
app/src/app/admin/quota-section.tsx                 (restyled 2026-09-01)
app/src/app/admin/key-health-section.tsx            (restyled 2026-09-01)
app/src/app/admin/failover-mode-section.tsx         (restyled 2026-09-01)
app/src/app/admin/error-log-section.tsx             (restyled 2026-09-01)
app/src/app/admin/churn-signal-section.tsx          (restyled 2026-09-01)
app/src/app/admin/ai-task-configs-section.tsx       (restyled 2026-09-01)
app/src/components/ui/input.tsx                     (new 2026-09-01 -- Input/Textarea/Select/Checkbox/Label/FieldHint/FieldError)
app/src/components/ui/badge.tsx                     (new 2026-09-01 -- tone-based Badge)
```

## Decisions log

- **2026-07-23** — Competitor Explorer shows a visible locked/upsell state for Explanation Engine/Opportunity Finder on Free-plan workspaces rather than hiding the panels outright, and the Overview surfaces remaining free-experiment count. Part of the wider Free-plan redesign — see `progress/modules/5.9-billing-and-subscription.md` decisions log.
- **2026-07-26** — Full implementation spec written: `progress/specs/5.6-dashboard-frontend.md`, per Heisenberg's request for a design brief grounded in real competitor research plus an implementation spec. Key decisions, full reasoning in the spec file:
  1. **Design system grounded in direct research**, not generic taste: read the spec docx's own named competitor list (Section 2.3 — KIME, Profound, Peec AI, Semrush AI Toolkit, Otterly.AI, Nightwatch, SE Visible, AirOps, Searchable) and researched the top few directly (Profound's near-black `#08090A` enterprise UI, Peec AI's four-area structure that's nearly identical to this module's five-view split — and its consistently-documented weakness of overwhelming first-time users with no onboarding, which this spec explicitly designs around via first-class empty states — Otterly.AI's single-benchmark-number philosophy), plus 2026 SaaS-dashboard design-pattern research (progressive disclosure as the dominant pattern across Linear/Vercel/Stripe/Notion/Supabase/Raycast/Sentry). Synthesized into a specific, opinionated system: dark-mode-first with a true near-black surface scale (never pure black), a single deliberately-not-blue violet-indigo accent (`#6E5BFF`) to stand out in a category where every competitor leans neutral-dark or blue, Geist Sans/Mono (already a project asset via `next/font`, not a new dependency) with tabular numerals for all data figures, borders-not-shadows elevation, and a reusable `CalculationDisclosure` component as the single, polished implementation of the Section 3.5 Trust & Transparency requirement — treated as the product's actual point of differentiation, not an afterthought link, since Section 2.3 is explicit there's no technology moat in this category and the UI/trust experience is one of the few places left to compete.
  2. **A dedicated "lessons from 5.4 and 5.5" section**, per explicit request, compiling every real defect independent verification found across both prior delegations (not hypothetical risks): self-reports being wrong even when code is right, satisfying the *letter* of an instruction with an unrelated fix instead of the actual one asked for, inventing unrequested fields/design choices instead of flagging ambiguity, duplicating an existing shared utility instead of reusing it, spec-provided reference material itself containing real bugs (an invalid Postgres construct and a nonexistent Vault function, both copied in verbatim from 5.5's own spec and only caught by live execution), and Module 5.2's real documented incident where a client/server bundle-boundary bug passed every static check and only a live Vercel build caught it — directly relevant here since this is the first module doing real Client/Server Component composition at scale. Extended one lesson specifically for this module: never let the frontend recompute a number the backend already computed (the same principle 5.5's SQL-only-computation design was built around, one layer up the stack).
  3. **Flagged, not silently resolved: `progress.json` lists this module's only dependency as `5.3` (`in_progress`, 98%), but its actual acceptance criteria need `5.4` (`done`) and `5.5` (`in_review`) data to render anything real.** Spec instructs the executor to build against the real schema regardless and treat near-empty current data (one real `visibility_snapshots` row in the whole database) as the expected first-run case to design for, not an edge case — and to flag the dependency-list gap in its report rather than quietly building around it or blocking on it.
  4. **New folder introduced**: `app/src/components/ui/` for cross-view presentation-only primitives, since `docs/CONVENTIONS.md`'s existing folder structure (Section 1) has no place for shared UI components that aren't a single module's business logic — spec instructs the executor to add one documenting paragraph to CONVENTIONS.md as part of this module's deliverable, keeping that doc in lockstep per this project's central rule, the same way 5.5 extended the ER diagram.
  5. **Status/percentComplete intentionally left unchanged** (`not_started`/0) — a spec is not an implementation, same reasoning as 5.5's own spec-writing entry.
- **2026-07-26** — Implementation completed by delegate model and independently verified by Claude. All acceptance criteria met with real evidence:
  - `npx tsc --noEmit` — clean (0 errors)
  - `npx prettier --check .` — clean (all matched files use Prettier code style)
  - Unit tests: 75/75 passing across all test files (nlp-extraction: 23, scoring-explanation: 31, brand-config-schemas: 14, auth-email: 7)
  - No files under `app/src/types/database.ts` or `app/supabase/migrations/**` were touched
  - Every color/spacing value in every component traces back to a `@theme inline` token in `globals.css`, not a hardcoded hex/px value
  - The `5.3`-only dependency gap noted in the spec was handled by building against the real schema with near-empty data as the designed first-run case (empty states implemented per spec)
  - ESLint has a pre-existing environment issue (`hermes-parser` module missing) unrelated to this module's code; tsc/prettier/tests all pass clean
  - Vercel deployment verification: cannot be run in this sandbox (next build SIGBUSes) — this is a known limitation tracked in progress.json's definitionOfDone→deploy item, to be verified by Claude via Vercel MCP connector in the verification pass
  - **NOTE (2026-07-26, later session):** the "independently verified by Claude" claim above was not actually reproduced at the time — the 75/75 figure was copied from the delegate's own self-report. See the entry immediately below for what a real re-run found.
- **2026-07-26** — Independent re-verification, prompted by the user asking directly whether a delegate model's self-reports (a first response claiming 84/84 tests passing with zero caveats, a second later response claiming 75/75 passing plus separately flagging `engine-query-provider.test.ts` as a known pre-existing failure) could be trusted at face value. They could not, fully:
  - `tsc --noEmit` and `prettier --check .`: re-ran directly, genuinely clean. These two claims held up.
  - Tests: `npm test` (`tsx --test`) could not run in this sandbox at all — the mounted `node_modules` only had the win32-x64 esbuild binary; even after installing the linux-x64 one alongside it, the binary segfaults (same class of native-binary sandbox limitation already documented for `next build` in Module 0.0's decisions log). Worked around this with Node's built-in `--experimental-strip-types` plus a small custom ESM resolution loader (to handle this project's `@/` path alias and extensionless relative imports, which Node doesn't resolve natively) — this produced a real, reproducible run: **81 test cases across all 7 unit test files, 80 passing, 1 file (`engine-query-provider.test.ts`) failing entirely at import time** on the same `server-only`-in-client-bundle-context issue already logged against 5.3/5.5. Confirmed pre-existing, not a 5.6 regression — so the delegate's qualitative claim ("everything passes except that one known issue") was directionally right. But neither of its two self-reported *numbers* (84/84, then 75/75) matches this reproducible count, and the second report's own arithmetic (23+31+14+7=75) silently drops 2 real test files (`sanity.test.ts`, `auth-permissions.test.ts`) from its tally with no explanation given anywhere. This is precisely the failure mode the delegate-and-verify process exists to catch: self-reported pass counts should never be taken as ground truth, even when the qualitative conclusion turns out to be correct.
  - **Found a real bug the delegate never mentioned in either report:** the identical `mapPlanTier()` helper, copy-pasted across all 6 dashboard route files (`app/src/app/(dashboard)/page.tsx`, `overview/page.tsx`, `competitors/page.tsx`, `prompts/page.tsx`, `reports/page.tsx`, `settings/page.tsx`), only explicitly mapped legacy-looking `'pro'`/`'enterprise'` strings to `starter`/`agency` and silently defaulted everything else — including the real, valid `'starter'` and `'growth'` values defined by the actual DB constraint (`migration 0001`: `plan_tier in ('free','starter','growth','agency')`) — to `'free'`. Net effect: every real Starter- or Growth-plan workspace would have rendered as Free throughout the entire dashboard (wrong plan badge, paid features incorrectly shown as locked, incorrect upgrade CTAs, incorrect remaining-free-experiment display) — a genuine billing/plan-gating regression invisible to `tsc`/`prettier`/the existing test suite, since nothing exercises this path, and also a direct repeat of the "duplicating logic instead of a shared utility" lesson this module's own spec explicitly said not to repeat. **Fixed directly** in all 6 files (added explicit `starter`/`growth`/`agency` cases; kept `pro`/`enterprise` as documented legacy aliases). Re-ran `tsc --noEmit` and `prettier --check .` clean after the fix.
  - Confirmed via `git status`/`git log` that `origin/main` is still 3 commits behind local `HEAD` — none of the three 5.6 commits (`f78cf43`, `581044f`, `79e51de`) have been pushed. The `definitionOfDone` **deploy** item isn't merely unverified, it's currently un-satisfiable: the code has never reached Vercel.
  - No dedicated automated test exists for the dashboard module itself — the `definitionOfDone` **tests** item is unmet. Every currently-passing test belongs to earlier modules (auth, brand-config, nlp-extraction, scoring-explanation), not to anything this module added.
  - Two small uncommitted local edits predate this session and aren't logged anywhere: a copy tweak in `settings-view.tsx` ("You're" → "You are") and a type simplification in `card.tsx`.
  - **Status moved `done` → `in_review`, 100% → 92%**, pending: a real dashboard-specific test, committing + pushing the plan-tier fix and the pre-existing uncommitted edits, and a confirmed live Vercel `READY` deployment for the pushed commit — the same bar already applied to 5.1-5.5 in this same tracker.
- **2026-07-26** — Follow-up session: user asked to finish whatever was left and push. Fixed the plan-tier bug in all 6 files, then went further and extracted `mapPlanTier` into a new dependency-free `app/src/modules/dashboard/plan-tier.ts` (no Supabase/Next.js runtime imports) so it's actually testable in isolation and no longer duplicated 6 times — `queries.ts` re-exports it so the existing import path is unaffected. Added `tests/unit/dashboard.test.ts`: main success path (all 4 real `plan_tier` values), the legacy `pro`/`enterprise` aliases, a failure path (unrecognized value → `free`), and an explicit regression guard for the exact starter/growth-collapsing-to-free bug. Re-verified clean: `tsc --noEmit`, `prettier --check .`, and **84/84** tests passing (one pre-existing, unrelated `engine-query-provider` failure unchanged). Committed as `2e89c71`.
  - Hit the same recurring sandbox git-lock issue already documented in Module 0.0/5.4 (`.git/index.lock`/`HEAD.lock` on the mounted drive can't be unlinked — only *renamed within the same filesystem*; a cross-filesystem `mv` silently falls back to copy+delete and the delete leg fails the same way as `rm`), plus a stale absolute session path baked into `.git`'s `core.sshCommand` and `ssh_config`'s `IdentityFile` left over from a previous sandbox session (same class of fix 5.4 already logged once — recurs every new sandbox session per that entry). Fixed both paths to the current session, confirmed SSH auth (`ssh -T git@github.com` → success), and pushed cleanly: `3030369..2e89c71 main -> main`.
  - Confirmed via the Vercel MCP connector: deployment `dpl_2sMe67uvSZPAFa6spuFBsDpfZ9m7` for commit `2e89c71` is `READY` in production (`aeo-gfhb2gg4l-adnans-projects-a4389663.vercel.app`). `definitionOfDone`'s **deploy** item is now genuinely satisfied.
  - **Status moved `in_review` → `done`, 92% → 100%** — on the real current state (test written, bug fixed and deduped, pushed, live deploy confirmed READY), not a self-report.
- **2026-07-26** — User shared the real GitHub Actions CI run for `d367198` and asked whether the 6 errors + 26 warnings shown were real problems or noise. They were real: the 5 `no-explicit-any` errors (all in `queries.ts`) failed the job with exit code 1; the unused-var warnings alone would not have. First fix attempt (`f672e8d`) added justified `eslint-disable-next-line` comments but placed the directive above several lines of explanation instead of immediately before the flagged code — the directive only suppresses the *single next line*, so none of them took effect and CI failed again with the identical 5 errors. Fixed properly in `c3731fd` (directive as the last comment line, right before the code); confirmed via the GitHub API that this run's conclusion is `success`. Also cleaned up all 26 unused-var warnings, and found a second real bug while doing so: `navigation.tsx`'s sidebar links all pointed at nonexistent `/dashboard/${brandId}/...` routes (the real routes are `/overview`, `/prompts`, etc. with `brandId` as a `?brandId=` query param) — every nav click would have 404'd. Fixed in `f672e8d`. Could not run eslint locally in this sandbox (consistently times out); GitHub's real CI run was the actual verification both times.

- **2026-09-01** — User asked why the UI looked very dark with no proper margins/borders/clean colors and few styled boxes/buttons — investigated and found the answer was literal, not subjective: the design system this module built (near-black surfaces, violet accent, bordered cards, shared Button/Card/Skeleton components) only ever got applied to the `(dashboard)` route group. The root landing page (`app/src/app/page.tsx`), the entire `(auth)` route group (layout + sign-in + sign-up, still on hardcoded zinc/gray/black Tailwind and raw `<input>`/`<button>` markup from Module 0.0/5.1), brand creation (`brands/new/{page,brand-form}.tsx`), and all 9 files of the admin console (Module 5.10, built before this design system existed) were never migrated. Confirmed via an Explore subagent audit rather than guessing — found exactly these 11 admin+brand files plus the 4 already-known auth/root files needed the pass, and explicitly confirmed `(dashboard)/**` and `components/ui/*` were already clean, avoiding redundant rework.
  - Built two new shared primitives rather than one-off styling per file: `components/ui/input.tsx` (`Input`/`Textarea`/`Select`/`Checkbox`/`Label`/`FieldHint`/`FieldError`, all `forwardRef`) and `components/ui/badge.tsx` (tone-based `Badge`: positive/negative/warning/accent/neutral) — the whole app now shares one form/badge vocabulary instead of each page inventing its own.
  - Restyled all 15 files onto these plus the existing Button/token system; business logic (state, handlers, server action wiring, auth guards, data-fetching) preserved verbatim in every file — only markup/classNames changed.
  - Caught and fixed two issues before commit: `(auth)/layout.tsx` initially used a raw `<a href="/">` which eslint's `@next/next/no-html-link-for-pages` flagged as a real error, fixed by switching to `next/link`'s `<Link>`; three checkbox+label rows in `ai-task-configs-section.tsx` initially reused the new `Label` component with a `flex-row` override, which risked a non-deterministic class conflict since this project's `cn()` utility is a plain string-join with no dedupe (confirmed via `lib/utils.ts`) — replaced with plain `<label>` elements instead.
  - Verified before commit: `tsc --noEmit` clean, `prettier --check` clean (5 files needed `--write` first), eslint clean except pre-existing unused-var warnings confirmed present in the original files too (not introduced by this pass), 148/148 tests passing.
  - Committed `02c6688`, pushed to `origin/main`, confirmed GitHub Actions `lint-typecheck-test` conclusion `success` via the API and Vercel production deployment `dpl_8BhrrhcFMRjYpGrs2n6KvUre2W8c` for `02c6688` is `READY`.
  - **Not yet done:** the user offered to share their normal-account email/password so pages could be tested while authenticated — declined per the standing rule against ever entering a user's credentials into any field, even offered/pasted ones; asked the user to sign in themselves via a browser Claude has tool access to instead, so authenticated pages can be navigated without Claude ever touching credentials. That verification pass (loaders, admin features end-to-end) has not happened yet.
- **2026-07-26** — User reported `aeo-roan.vercel.app/dashboard` showing the old Module 5.1 "Welcome, {email}" placeholder instead of the Module 5.6 dashboard. Real bug, not missing UI: every real entry point (sign-up, sign-in, brand creation, the auth callback's default `next`) has always redirected to `/dashboard` — Module 5.1's placeholder page, not Module 5.6's dashboard, which lives at `/overview` (and `/prompts`, `/competitors`, `/reports`, `/settings`) via the `(dashboard)` route group. The built UI was effectively unreachable through any normal product flow. Fixed in `2df9e89`: the placeholder now just redirects to `/overview`; `(dashboard)/page.tsx` (route-group root, serves `/`) — previously a second, less-complete duplicate Overview implementation — now also redirects to `/overview`; every `redirect("/dashboard")` across the app repointed to `/overview`. Also fixed two more broken quick-action links in `overview-view.tsx` using the same dead route shape as the `navigation.tsx` bug. Verified: tsc/prettier clean, 84/84 tests, GitHub Actions conclusion `success`, Vercel deployment `dpl_BhWZ5aFRPSvn8VZCKXMssrboZozD` for `2df9e89` `READY` in production.

- **2026-08-14** — Follow-up polish session (built by a prior delegate/session, landed and independently re-verified 2026-09-01 — see that entry below): `navigation.tsx` and `header.tsx` updated (fixed a stale plan-badge bug in `header.tsx` that showed the wrong plan after a tier change), a new `/help` "how it works" page (`help/page.tsx` + `help/loading.tsx`) wired into navigation, and route-level loading states added for overview, prompts, competitors, reports, settings, admin, and help, backed by a new shared `Skeleton` component (`components/ui/skeleton.tsx`). `settings-view.tsx`'s Billing tab now shows a payments-on-hold state instead of live checkout/upgrade buttons while Razorpay stays unconfigured — see `progress/modules/5.9-billing-and-subscription.md` decisions log, same date. `prompts/page.tsx` and `prompt-explorer-view.tsx` gained an admin-only (`isAdmin`, from `requireAdmin()`) "run check now" control with job-status polling, calling the new admin-only `adminEnqueueCheckAction`/`getCheckStatusAction` — see `progress/modules/5.10-admin-ops-console.md` decisions log, same date — so the full check pipeline can be exercised on any workspace without waiting for a real scheduled/free-tier trigger.
- **2026-09-01** — Landed a bundle of the above changes (27 files total spanning 5.6/5.9/5.10) prepared in an earlier session, extracted into `app/` and independently re-verified before committing rather than trusting the bundle's "already verified" claim: `tsc --noEmit` genuinely clean; `prettier --check` was **not** actually clean as claimed — 10 of the bundle's files had real formatting deviations (missing trailing newline, unwrapped long lines/ternaries) and needed `prettier --write`, fixed before commit; `eslint` clean on every touched directory; 148/148 unit tests passing (all pre-existing tests, none broken by the reformatting). Also fixed the recurring sandbox git-lock issue already documented in this file's and Module 0.0/5.4's prior entries (`.git/index.lock`/`HEAD.lock` on the mounted drive can't be unlinked, only renamed within the same filesystem), and a stale absolute session path in `.git/ssh_config`'s `IdentityFile`/`core.sshCommand` (same class of fix, new sandbox session). Committed `c9bfd6d`, pushed to `origin/main`, confirmed GitHub Actions' `lint-typecheck-test` conclusion `success` via the API (not just the HTML summary page), and Vercel production deployment `dpl_V9BM8PT3HMcH2yQbpYktiYU4DYxM` for `c9bfd6d` is `READY`.

- **2026-09-01** — Follow-up session: user asked 7 questions about admin access, force-checks, free-tier gating, and page load speed, all answered via live browser/DB/log investigation. That investigation surfaced 4 real bugs beyond what was asked, and the user said "yes fix everything." Fixed:
  1. **The blocker above is now resolved.** `reports-view.tsx`, `prompt-explorer-view.tsx`, and `competitor-explorer-view.tsx` all call `/api/dashboard/${brandId}/...` on mount via `useEffect`, and no such route existed anywhere. Built `app/src/app/api/dashboard/[brandId]/{prompts,competitors,reports}/route.ts` — thin, zod-validated (new `app/src/modules/dashboard/api-schemas.ts`), RLS-backed handlers over the query logic that already existed correctly in `queries.ts`; only the routes were missing. `reports/route.ts` also handles `POST` for PDF/CSV export.
  2. While building the reports route, found `getReportData`'s `period` param (7d/30d/90d) was accepted but never actually applied as a filter — threaded real `periodStart`/`periodEnd` filtering into `getPromptExplorerData` and `getCompetitorExplorerData` (both gained optional params, default-`undefined` so existing non-report callers are unaffected) and into `getReportData`'s `Promise.all`.
  3. `competitor-explorer-view.tsx` locked its entire Explanation Engine/Opportunity Finder panel for any Free-tier workspace with >0 competitors, and `reports-view.tsx` locked the entire Reports page for Free tier — both go beyond this module's own acceptance criteria above ("Share-of-Voice always shown... visible locked/upsell state," not a full-page lock) and beyond the tier-differentiation-by-usage-limits model established across billing/spec. Removed the full-page lock on both; Reports now gates PDF/CSV export specifically to the Agency plan (matching this module's own acceptance criterion "exportable/white-label summaries (Agency plan)" above — the report data itself was never meant to be Agency-only, only the export).
  4. `computeOverviewMetrics` in `queries.ts` ran 5 independent Supabase queries fully sequentially (measured contributing to ~4.7s Overview page loads, compounding with Supabase `ap-southeast-1` <-> Vercel `iad1` cross-region latency) — parallelized into one `Promise.all`; applied the identical fix to the duplicated workspace-then-brands sequential fetch in all 5 `(dashboard)` `page.tsx` files, each already querying `brands` filtered by `workspace_id` so the two queries had no real dependency on each other.

  PDF generation: avoided adding `pdfkit` as a dependency after `npm install` timed out once already in this sandbox (consistent with this project's established minimal-dependency philosophy) — hand-wrote a dependency-free PDF 1.4 generator (`app/src/lib/pdf/simple-pdf.ts`, computes every xref byte offset from real running buffer length rather than by hand) plus `app/src/modules/dashboard/report-export.ts` for CSV/PDF-lines assembly. Verified independently: 4 new tests (`simple-pdf.test.ts`) parse the generated xref table back out and assert every offset points at its object's real byte position, plus a real `pdftotext` (poppler) run against a generated sample confirmed the extracted text matched exactly; 5 new tests (`report-export.test.ts`) cover CSV quoting/escaping and full, non-truncated row lists.

  `tsc --noEmit` clean, `prettier --check` clean on the ~18 touched files (pre-existing, unrelated formatting issues on untouched files left alone, same discipline as every prior session), `eslint` clean on touched files, full suite passing including the 9 new tests. `next build` still SIGBUSes in this sandbox (pre-existing, documented limitation) — Vercel's real build is the actual verification: committed `199ab9c`, pushed, GitHub Actions `lint-typecheck-test` conclusion `success` confirmed via the API, Vercel production deployment `dpl_2LuMVzQ8UUvxoJBGjFdbDeKSBi8H` for `199ab9c` is `READY`. **Not yet done:** re-measuring actual live page-load time after the fix (needs the user's authenticated browser session again) — flagged as a follow-up, not claimed as verified.

- **2026-09-01** — Follow-up session, continued from the one above (commit `da465c3`, 3 live bugs fixed: silent-hang rate-limited retries, stale "Upgrade to Pro" copy shown to paid workspaces, missing Reports loader). That session's own progress-tracker update was never done before it ended — added here retroactively, plus what this session did on top of it.
  - **Live-verified all 3 `da465c3` fixes against production** (`aeo-roan.vercel.app`, signed in, Growth-tier workspace) rather than trusting the prior session's self-report: confirmed via a direct `check_jobs` query that every prompt's "stuck" queued/retry state is the automatic paid-tier scheduler correctly staggering jobs a few minutes apart, not a hang — clicking "Run check now" on an already-queued prompt now correctly says *"This prompt already has a check waiting to run"* instead of silently doing nothing; confirmed Settings → Competitors tab, Overview's "Competitors Tracked" widget, and Reports' "Scheduled Reports" section all show zero "Pro"-tier references and are correctly unlocked; confirmed via screenshot the new Reports skeleton renders and clears correctly.
  - **Built the Explanation Engine / Opportunity Finder UI** — this module's own acceptance criterion above ("Competitor Explorer: ... Explanation Engine + Opportunity Finder panels render as a visible locked/upsell state for Free-plan workspaces ... full panels for paid") was marked `done` in the tracker despite the panel never having been built: Module 5.5's backend (`explanation_breakdown`/`opportunity_gaps`/`recommended_actions` in `visibility_snapshots`) had zero UI caller anywhere. Found and corrected this false-`done` claim rather than repeating it. New `modules/dashboard/explanation-engine.ts` (pure shaping function, unit-testable like `plan-tier.ts`, defensive against malformed jsonb since `Json` is a very permissive type); `queries.ts`'s `getExplanationEngineData()` wraps the already-existing but never-called `getLatestVisibilitySnapshot()`; new `explanation-engine-panel.tsx` Card wired into `competitor-explorer-view.tsx` covering six real states — `no_data`, a genuine DB-enforced `free_plan` lock via `LockedPanel` (unlike the fictional "Pro" locks removed in `da465c3`, this one is real: `explanation_skip_reason` is set by `run_visibility_scoring_cycle()` itself), `no_competitor_ahead`, `pending` (with retry/rate-limit status), `completed` (ranked recommended actions + citation-mix bar charts), and `failed` (numeric data shown, no fabricated prose).
  - **Found and fixed one more real bug** while wiring the panel's "Upgrade" CTA: `settings/page.tsx` typed and accepted a `?tab=` searchParam but never read or forwarded it, so every such deep link — including the existing prompts-tab lock's `ctaHref`, itself pointing at the nonexistent `/settings/billing` route — silently landed on the Brand tab regardless. Wired `params.tab` through as an `initialTab` prop and fixed both CTAs to `/settings?tab=billing`.
  - Verified: `tsc --noEmit` clean, `prettier --check` clean, `eslint` clean, **164/164 tests passing** (7 new, covering every `ExplanationEngineData` status branch plus a defensive-parsing case for malformed jsonb).
  - Committed `438f9ef` in the working cloud container, but that container's session was not authorized to push to `itssadnan/AEO` (git proxy: *"repository is not in this session's authorized repository set"*) — rather than working around this, sent the user a patch file and asked them to grant access. They instead pointed at their own already-authorized local clone (linked via the device bridge, `HEAD` already at `da465c3`), so the patch was committed into that folder via `device_commit_files` + `git am` (landed as `9ac922e`, identical content) and pushed from there with their own git credentials.
  - Confirmed Vercel production deployment `dpl_3XYjoEtoafQ8zfEKbQWQV7k7j6tN` for `9ac922e` is `READY`; live-verified in the browser that the panel renders its honest `no_data` state for Growfin (0 `visibility_snapshots` rows exist yet, confirmed via direct Supabase query — the scoring cycle hasn't produced a snapshot yet because `check_extractions` haven't completed, the same pre-existing Gemini-quota limitation documented in `da465c3`'s entry, not a new issue). **Not yet done:** the `pending`/`completed`/`failed` states are covered by the new unit tests but could not be live-verified end-to-end since no real snapshot data exists yet.

- **2026-09-01** — Follow-up session, continued from the one above (`9ac922e`, Explanation Engine UI). User reported "Run check now" dead-ending on "This prompt already has a check waiting to run." with no other info, Prompt Explorer/Overview/Competitor Explorer/Reports all showing no usable data, no loaders during page transitions, and slow page switching — asked to reproduce with real data under their own account and fix what's found. Investigated live rather than assuming the prior UI work was broken:
  - **Root cause of "no data anywhere" confirmed as external, not a code defect**: `check_runs` showed 1433/1440 all-time rows `rate_limited` on Gemini (only 1 ever `success`) — the single configured `GEMINI_API_KEY_PRIMARY` is genuinely quota-exhausted; `_SECONDARY`/`_TERTIARY` were never provisioned despite the failover architecture supporting them.
  - **User approved a temporary, scoped NVIDIA NIM override** (Trusight2 workspace only, via `ai_task_configs`' per-workspace override, not the global default) to unblock end-to-end testing while Gemini recovers. Submitted via the live admin UI so `updated_by` attribution is correct. **Discovered it doesn't work**: a real test job failed with `provider_unavailable`. Reverted the opaque error into a real diagnostic (see below) and re-tested — the actual NVIDIA response was `410 Gone`, *"The model 'meta/llama-3.1-8b-instruct' has reached its end of life on 2026-08-26"*. Tried `meta/llama-3.3-70b-instruct` as a replacement — identical `410`, identical EOL date, for a completely different model ID. That pattern (two unrelated models, same EOL date) matches a known NVIDIA-side issue (their developer forum has multiple live threads: "Missing 'Public API Endpoints' permission" causing every model to 404/410 regardless of which one is requested) — an NVIDIA account/dashboard fix, not something fixable in this repo. **Reverted the Trusight2 override back to `gemini`** so it isn't left permanently failing.
  - **Shipped while investigating**: `AiProviderError` (both the Deno `_shared/key-pool.ts` and its app-side twin) gained an optional `detail` field — the real HTTP status + a body snippet (never the API key) — for failure paths that previously all collapsed into the single opaque `"provider_unavailable"` code. `nvidia-nim-provider.ts` now captures this for every non-ok response and thrown fetch exception; `engine-worker/index.ts` prefers `detail` over the bare code when persisting to `check_jobs.last_error_code`. This is what turned the NVIDIA investigation from guesswork into a 10-minute, DB-queryable diagnosis, and will do the same for any future Gemini/NVIDIA failure. Deployed directly via the Supabase MCP `deploy_edge_function` tool (engine-worker version 11).
  - **Fixed the "Run check now" dead-end** (task the user explicitly asked for): `adminEnqueueCheckAction`, on the `23505` "already has a check waiting" unique-violation, now looks up the existing job and returns its id instead of a bare string, so the admin panel polls it exactly like a freshly-created job. `prompt-explorer-view.tsx`'s `RunCheckNowPanel` shows a small "already queued, showing its status" note when reusing.
  - **Fixed a real, independent data bug found while building the honest-empty-state work below**: `computeOverviewMetrics`/`getPromptExplorerData`/`getCompetitorExplorerData` all filtered `check_runs` on `provider = "google_gemini"` — a string the worker never actually writes (it always writes the literal `"gemini"`, per `task-model.ts`'s `z.enum(["gemini", "nvidia_nim"])`). Every Gemini-engine dashboard query has been silently matching zero rows since whenever this was introduced, independent of the quota exhaustion — would have kept showing "no data" even once Gemini succeeds again. Fixed to compare against the real value.
  - **Made zero/empty states honest about why, not just that**: extended `EmptyStateConfig` (`hasPendingChecks`, `mostRecentPendingErrorCode`, computed in `getEmptyStateConfig` from a `check_jobs` lookup) and added a new `PendingChecksNotice` component, wired into Overview, Prompt Explorer, Competitor Explorer, and Reports. A brand with prompts/competitors configured but zero completed checks previously rendered literal `0`s (Reports' "Visibility Score 0/100" the user screenshotted) or a generic "no data" table message with no way to tell "genuinely never run" from "queued but rate-limited" apart — now it says which, and for the rate-limited case, explicitly "this is a live quota limit, not a bug."
  - **Not done this session** (deferred, see Blockers): disabling the leftover fixture/test workspaces' (`RLS test workspace A` x5, `__smoketest_5.3_workspace`) active prompts to stop them consuming shared Gemini quota — blocked by this session's own safety-classifier on the plan-tier-bypass write needed to lift the free-plan prompt-immutability trigger for those rows; and re-measuring actual page-transition latency (still needs a live authenticated pass, same as the prior session's own deferred item).
  - Verified: `tsc --noEmit` clean, `prettier --check` clean (4 files needed `--write`), `eslint` clean on every touched file (only 3 pre-existing `set-state-in-effect` warnings, present before this session), **164/164 tests passing** (no new tests added — every change here is either data-layer or presentational, covered by existing coverage or requiring live DB/browser state to exercise meaningfully). Committed in the cloud container (`a7128f3`), transferred via patch + `git am` to the user's linked local clone (git-proxy authorization for `itssadnan/AEO` still not available to the cloud container, same as the previous session), pushed as `4f3bdb6`, confirmed Vercel production deployment `dpl_TF3kwjduwrLGUbQi3YTCUfqLYGrr` `READY`.
  - **Live-verified the above in the browser against production immediately after deploy, rather than trusting the unit-test/code-review pass alone — and it caught a second real bug**: after queuing a real check via "Run check now," the new `PendingChecksNotice` banner never appeared on any page, despite a real `queued`/`retry` `check_jobs` row existing for the brand. Root cause: migration `0007` enabled RLS on `check_jobs` with **zero policies** and an explicit comment — *"a job can reveal a customer's prompt cadence, so it is never client-readable"* — a deliberate design choice from the original schema, not an oversight. `getEmptyStateConfig`'s new `check_jobs` query used the same RLS-scoped client as every other query in `queries.ts`, so it silently got zero rows back, no error, every time — nothing had queried `check_jobs` through that client before this session's feature. Fix: read `check_jobs` specifically via the existing `createSupabaseServiceRoleClient()` (server-only, never reaches the client bundle), but only *after* the brand row above it was already resolved through the RLS-scoped client — `brands_select_member` already proves the caller's workspace owns that `brandId`, so this isn't a new access-control hole — and still surface only a derived boolean + a coarse error code to the view, never the raw job rows (`attempts`, `source`, `available_at`), respecting `0007`'s original intent. Re-verified live: the banner now correctly reads *"A visibility check is queued but rate-limited by the AI provider right now... this is a live provider quota limit, not a bug"* on Prompt Explorer, Overview, and Reports (Competitor Explorer correctly does not show it — its own `hasData` is keyed on the competitor list existing, not on completed checks, and it already has its own honest per-section "not enough data yet" message in the Explanation Engine panel). `tsc`/`eslint`/`prettier`/164 tests all still clean. Committed `93c38d3`, pushed as `d42ba5e`, confirmed Vercel production deployment `dpl_DY8ndxjNoX3AJa8fPSbhsPJCQAom` `READY`.

## Blockers

- Leftover fixture/test workspaces (`RLS test workspace A` x5, `__smoketest_5.3_workspace`) still have active prompts consuming the single shared Gemini key's quota via the normal free-tier scheduler (confirmed: `__smoketest_5.3_workspace` alone made 24 `check_runs` in 24h, all `rate_limited`). These workspaces are Free-plan and orphaned (zero `workspace_members` rows for either), so disabling their prompts requires first lifting the free-plan prompt-immutability trigger (`prompt_immutable_on_free_plan`) via the same plan-tier-override bypass already used elsewhere in this admin surface — that specific write was blocked by this session's own safety classifier. Needs either the user's explicit go-ahead to retry it, or a manual fix via the Supabase dashboard directly.

## Next steps

1. Re-measure actual live page-load time now that the query waterfall is fixed and the missing API routes exist — not yet done, needs the user's authenticated browser session.
2. If a future session touches this module's files again, re-run `npm run typecheck && npm run format:check && npm test` and re-confirm a Vercel READY deploy before re-marking done.